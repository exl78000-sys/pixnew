# 英超戰情室 —— 給接手 AI 助手的常駐規則

專案在 `epl/`。這份是**工作規則**;專案在做什麼看 `epl/README.md`,
這個時間點的狀態看 `epl/docs/接手資訊.md`,
**接下來要做什麼看 `epl/docs/補齊規劃.md`**(唯一一份待辦,不要在別處再開一份)。

**任何程式、資料流程、工作流程或架構更動都要同步寫文件。** 至少更新
`epl/docs/變更紀錄.md`;若會影響操作、故障處理或接手判斷,也要更新對應主題文件。
不能只改程式、靠 commit message 代替文件。

**已經是六個聯賽**:英超(根目錄的資料集)、西甲(`web/data/leagues/es1/`)、
英冠(`web/data/leagues/en2/`)、德甲(`de1/`)、義甲(`it1/`)、法甲(`fr1/`,後三個都是 2026-09-15 加的)。

**德甲、義甲、法甲共用三支實作**:`lib/league-fetch.mjs`(抓取)、`lib/build-league.mjs`(建站)、
`lib/test-league.mjs`(測試)—— 各自只剩一份參數。**英超西甲英冠刻意不併**:
英超有 FPL 與官方 feed、西甲有 SportMonks 補充層、英冠的球員層是逐場累加而且有升級附加賽,
那三個的形狀真的不同,硬塞會變成一堆 if,那比複製還糟。
義甲 FotMob id **55**、法甲 **53**、Understat 代號 `Serie_A` / `Ligue_1`,
全部是 `probe-new-leagues.mjs` 在 runner 上逐隊比對證明出來的(**巴西也有 Serie A**)。
隊徽 2026-09-16 補齊:義甲 **20/20**、法甲 **18/18**(`npm run it1:crests` / `fr1:crests`,
內嵌 data URI)。那一支的來源在 raw.githubusercontent.com,**沙箱放行,本機就跑得完**。
它們**還沒有**的是隊色、城市、球場與教練 —— 那幾樣要人工交付並過核對器。
**升降級規則逐聯賽不同,不可以照抄**:德甲法甲是第 16 名打跨聯賽附加賽、
義甲是後 3 名直接降級沒有附加賽。
德甲 2026-09-15 起有**球隊、比賽與球員**三層:球員層走 Understat(整季彙總,
一季一個請求,跟西甲**共用同一支適配器**),隊名對照用逐隊出賽分鐘與進球獨立核對過。
賽後報告與逐場統計走 FotMob 聯賽 id **54**(證明過:從 allLeagues 找 ccode=GER、
再逐隊比對名冊 18/18 —— **奧地利甲也叫 Bundesliga**,id 38 就在倉庫裡,挑錯照樣回得出
18 隊與逐場資料而畫面不報錯)。隊徽 **18/18**(`npm run de1:crests`,內嵌 data URI)。
它**還沒有**的是隊色、城市、球場、教練、傷停與即時比分;Understat 本身不給背號、頭貼、
出生日期與身價,所以年齡是 null、「22 歲以下」那張榜是空的 —— 由 `leaders.missing`
講出來,不留空欄位。**球員進球加總比積分榜少的那幾顆,2026-09-16 回填上一季之後對過了。**
六個季的逐場事件涵蓋現在全部 100%,結果是**兩個對上、四個留著殘差**:
德甲 2025-26(缺口 20 = 烏龍球 20)與義甲 2026-27(1 = 1)對上;
德甲 2026-27 差 1、義甲 2025-26 差 −1、法甲 2025-26 差 −1、法甲 2026-27 差 4。
build 逐季量三個數字,**涵蓋全部場次而且缺口等於烏龍球數**才算對上,兩個條件缺一不可;
沒對上的照實逐季寫在畫面上,而且分得出兩種理由(還沒涵蓋滿 / 涵蓋滿了但還差幾顆)。
這幾個數字會隨資料變 —— **引用就跑一次 build 看 log,不要抄這裡**。
**德甲缺的東西跟英冠缺的不是同一種**:英冠是「Understat 不涵蓋這個聯賽」
(實測過的否定,永遠不會有),德甲只是沙箱的出口代理不放行 understat.com、要在 runner 上抓。
這兩句對讀者的意義完全不同,不要互相抄。

**本站有兩種合法的球員層,前端看 `leaders.source` 分岔**:`match-aggregate`(英冠與盃賽,
逐場逐人統計累加)與 `Understat`(西甲與德甲,整季彙總)。兩者形狀本來就不同,
新接一個聯賽時**要跟同一種的那個聯賽比形狀**,不是跟隨便一個比。

**凡是「列出所有聯賽」的清單,一律掃 `web/data/leagues/` 目錄,不要手寫。**
手寫的代價已經付過兩次:資產戳那條漏了英冠(它的 meta 少了戳三個月沒有人知道),
加德甲時又有三份寫死的清單(prob-history、fixtures 欄位對照、sweep 的聯賽表)
會讓新聯賽**靜靜不被檢查**,而畫面完全正常。

**盃賽(足總盃、聯賽盃)2026-09-13 起有賽後報告,2026-09-15 起有球員榜。** 逐場詳情走 FotMob
(探測過:stats、shotmap、lineup、events、playerStats 五塊都有,跨三個分級都成立),接法跟歐冠同一條
(`lib/cup-details.mjs`、單場頁 `cup-match.html`);球員榜由逐場逐人統計累加
(`lib/season-players.mjs`,歐冠與英冠共用)。**但仍然沒有預測** —— 對手一半是第三、四級
球隊,本站沒有它們的賽果,評不出強度(鐵則二)。「盃賽只有賽果」「盃賽沒有球員資料」這兩句都已經不成立。
盃賽球員榜有三件事跟聯賽不同,畫面上都要講:低分級的場次供應商常常沒有逐人統計(足總盃 117 場裡只有 81 場有)、
比分核對是**同一家供應商**的一致性檢查(盃賽賽果本身就是 FotMob)、評分榜的門檻要比聯賽高(淘汰制)。

**荷甲 / 葡超不算「新聯賽」。** 2026-09-09 加的只有它們的**積分榜**
(`web/data/ucl-standings.json`,3 KB),而且只給歐冠那一頁的賽前對比用 ——
沒有球員層、沒有 xG、沒有預測,**導覽列不掛、球隊點不進去**。
(德甲 / 義甲 / 法甲原本也在這一句裡,2026-09-15 之後它們已經是完整聯賽了 ——
**這種「我們只有 X」的句子每加一個東西就要回來看一次**,那是本站犯過四次的坑。)
要把荷甲葡超做成完整聯賽是另一件事。新增聯賽的做法看 `scripts/build-serie-a.mjs`
與 `scripts/lib/build-league.mjs` / `lib/league-fetch.mjs` / `lib/test-league.mjs` ——
德甲義甲法甲共用那三支,新的一個只要寫一份參數;它們的檔頭寫了
「這個聯賽做不到什麼、為什麼」,那一段比程式本身重要。

**英冠沒有的是「整季的球員資料源」,不是球員資料。** Understat 不涵蓋英冠、FPL 只有英超
(實測過,對照組與端點見 `docs/英冠-資料來源探測.md`)——但 FotMob 逐場端點(聯賽 id 48)
給控球、球隊統計、逐射門 xG、事件、正式名單**與逐人統計**,所以:
比賽層從 2026-09-05 起有了(賽後報告、球隊頁的逐場統計);
**球員層 2026-09-15 起也有了**,由逐場逐人統計累加而成(`lib/season-players.mjs`,歐冠共用),
每一場都要「球員進球 + 對手烏龍球 = 本站賽果的比分」才計入,牌另外拿 football-data.co.uk 逐場核對。
「英冠沒有賽後資料源」「英冠沒有球員數據」這兩句都已經不成立,不要再寫回去。
它**仍然沒有**的:球員 xG 模型、身價、年齡、傷停、頭貼、即時比分。導覽列只掛做得出來的那幾頁。

---

## 一、鐵則(違反的話,做出來的東西整個沒有價值)

這個專案的賣點不是功能多,是**每個數字都查得到出處**。以下五條沒有例外。

### 1. 不准編數字

任何顯示在頁面上的數值,都必須是從真實資料算出來的。
不准用 LLM 猜、不准用「合理的估計值」填空、不准為了畫面好看造一個範例值。

AI 報告層(`epl/scripts/lib/report/`)有一道驗證器:文章裡出現的每個數字都要能對回
feature bundle,對不上整篇退回制式模板。**不要繞過它。**

### 2. 沒有回測證據就不進模型

想加一個預測特徵?先證明它有用:
- 調參與驗收要用**不同賽季**。同一批資料又調又驗,挑出來的一定是雜訊。
- 改善幅度要**大過成對比較的標準誤**,不然只是換一批比賽就會翻盤的波動。
- 參考 `npm run tune:form` 的做法。

已經量過而**沒有通過**的:近五戰狀況、近五戰進失球、歷來交手淨勝球。
它們的係數是 0(`epl/scripts/lib/form.mjs` 的 `TUNED`),只當資訊顯示。
`npm test` 有一條專門守著:套用之後 λ 必須一模一樣。**不要因為「直覺上應該有用」就打開。**

### 3. 拿不到的資料就不做,也不留空欄位

留一個永遠空白的欄位,比不做更糟 —— 讀者會以為是壞掉。

但**「探測失敗」不等於「資料不存在」**,這一條我親自踩過:

我曾經在這裡寫「進球方式(運動戰/角球/任意球)沒有免費來源,不做」,
理由是探測 `understat.com/team/Arsenal/2025` 回了 200 但解析不到資料變數。
**那是我端點找錯了** —— 資料在 `understat.com/getTeamData/{隊名}/{年份}`,
是獨立的 JSON 端點,不是嵌在球隊 HTML 頁裡。

現在英超與西甲的進球情境資料都已經在倉庫裡
(`data/raw/understat/` 與 `data/raw/understat-la-liga/`),
分成 `OpenPlay / FromCorner / SetPiece / DirectFreekick / Penalty` 五類,
而且核對過:Arsenal 五類相加 71 球等於該季總進球,角球 19 球對得上官網文章。

**但界線要守住:能做的是「球隊整季的分類」,不是「這一球是怎麼進的」。**
Understat 給的是球隊層級的季摘要,把某一類掛到某位球員的某一顆球是我們沒有的資料。
所以球員的逐球表**不要**加「進球方式」欄位,球隊整季分類放獨立面板。

**教訓:斷言某個資料拿不到之前,先確認自己試的是不是對的端點。**
真的確定拿不到再寫進這裡,而且要寫清楚試過什麼、用的是哪個網址。

### 4. 資料的不確定性要寫在畫面上

人工維護的、可能過期的、樣本不足的、推論而非實測的 —— 全部要標。
既有做法可參考:教練名冊的鮮度提醒、升班馬的「聯盟後段先驗」說明、
攻守分型的「這是推論,官方沒有這個東西」、模型頁的「測過但沒有進模型的特徵」。

### 5. 外部抽取的資料一定要用獨立來源核對

協作方自己回報「檢查全過」不算數。實際踩過:
交回來的進球明細自報 `scoreMismatches: 0`,但拿 openfootball 的賽果
(跟 FPL 完全獨立)逐場核對,2024-25 有 15 場、2025-26 有 24 場對不上。

---

## 二、會靜靜出錯的陷阱(每一條都真的踩過)

這些不會拋錯,只會給出錯的答案。

| 陷阱 | 症狀 | 正解 |
|---|---|---|
| `players_raw.csv` 是**季末快照** | 中途轉隊的人,整季的球記到新東家(Rashford 替曼聯進的 2 球變成維拉的) | 逐場資料要用逐場欄位。球員隊伍從 `opponent_team` + `was_home` + 賽程反推 |
| FPL 的 `element` 與 `opponent_team` **每季重新編號** | 用錯季的對照表 → 對手全錯 | 一定要用**同一季**的 `players_raw.csv` / `teams.csv` |
| FPL 的 `round` 是 **gameweek**,不是賽程輪次 | 改期的比賽對不上(AVL vs LIV:FPL GW25、賽程第 29 輪) | 配對用**日期優先**、輪次備援 |
| 官方 event 的 `type` 是**代碼**不是英文字 | 用 `/goal/i` 比對 → 一顆進球都抓不到 | 型別是 `G`/`B`/`S`/`PS`/`PE` |
| **烏龍球不是 `G` 事件** | 只認 `G` → 少算(Brighton 4-0 Aston Villa 只抓到 3 顆) | 用「**比分變了就是進球**」判定,不看型別 |
| **完場標記比真正進球事件早寫入** | 同一個最終比分先出現在 `PE`,後出現在 `G`;只按 `time.millis` 掃會讓 `PE` 認領進球,射手變 null。CHE 4-3 BHA 的 90+6 分實際發生過 | 先用比分找進球,再於同一比分事件中排除 `B/S/PS/PE`、優先選有射手的 `G/O`。完賽快取還要驗進球數、最後比分、射手,不完整就退避重抓 |
| **上游的牌事件可以沒有 `personId`**(教練或板凳人員吃牌) | 轉換器照設計留著、`team` 給 null;測試卻要求每筆都查得到隊伍 → `npm test` 紅、deploy 被擋兩天,手動重跑 4 次都一樣(NEW vs BOU 71',2026-09-05)。重抓也沒用,上游回的就是這筆 | 「查不到隊伍」分兩種:**有 person 卻查不到**才是本站的 bug;**沒有 person** 的留著、`team` null、前端印「不詳」。不要再把後者收緊成紅線 |
| 烏龍球事件的 `teamId` 語意不明確 | 實測一例是**得分方**(Lindelöf 替維拉踢進烏龍,teamId 卻是 Brighton),但樣本只有一個,不要當通則 | 別依賴 `teamId`。得分方一律由**比分差**決定,`goalsOf()` 就是這樣寫的 |
| openfootball 的隊名寫法**跨季不同** | `Manchester United` vs `Manchester United FC` → 整季資料被 tolerant 模式吞掉 | `codeOf` 已有寬鬆比對;tolerant 模式會把跳過的隊名印出來,**要看那行輸出** |
| FPL 的球隊 `short_name` 恰好等於本專案隊碼 | —— | 這是驗證過的,20 隊全對,可以直接用 |
| `versus()` 的「越低越好」取倒數 | 值是 0 時 1/0 爆掉,對面壓成一根針 | 分母加同量級緩衝(已修,有測試守著) |
| **同一次瀏覽裡一頁新版面、一頁舊版面** | 在導覽列點來點去,有時候跳成上一版的排版 —— 因為 GitHub Pages 給 HTML 的快取是十分鐘而且**每個檔案各自計時**,index.html 可能是舊的、teams.html 剛好過期換新的 | `stamp-assets.mjs` 把戳寫進 `meta.json` 的 `assets`,`core.js` 從 `import.meta.url` 讀自己的戳比對,對不上就重載一次(sessionStorage 記號防無限重載) |
| **import 那一行帶著版本戳** | 用字面字串 `from './core.js'` 做 replace **靜靜沒命中**;程式呼叫了沒 import 的東西 → 頁面顯示「載入失敗」,而 `npm test` 全綠(測試檢查不到版面) | `stamp-assets.mjs` 會把 import 改成 `from './core.js?v=abcd1234'`。改 import 那幾行一律用正則 `from '\./x\.js(\?v=[0-9a-f]{8})?'`,而且 replace 之後要斷言真的有命中 |
| 頁面切換後計時器沒清 | 舊頁面 30 秒後覆蓋 `#app`,看起來像「自動跳回去」 | 用 `C.pageInterval()`,不要裸 `setInterval` |
| Understat 的資料**不在 HTML 頁裡**(球隊頁與聯賽頁都是) | 抓 `/team/{隊}/{年}` 或 `/league/{聯賽}/{年}` 只回 18 KB 外殼,一個資料變數都沒有 | 球隊用 `/getTeamData/{隊}/{年}`;球員整季數據用 `POST /main/getPlayersStats/`,body 是 `league=La_liga&season=2025`,一個請求回整季 600 人 |
| **football-data.org 的 `fullTime` 在 PK 場是「累加值」不是比分** | 2025-26 歐冠決賽的 `fullTime` 是 `5-4`,實際上是 **1-1、PK 4-3** —— 直接印會把冠軍講錯 | 實測 6 場全部成立:`fullTime = regularTime + extraTime + penalties`。PK 場的比分要用 `regularTime + extraTime`,**不是 fullTime**。`duration` 是 `REGULAR` / `EXTRA_TIME` / `PENALTY_SHOOTOUT` |
| **FotMob 的比賽網址順序不是主客** | `/matches/{甲}-vs-{乙}/` 看起來像「甲是主隊」,實際上只有一半對得上(2024-25:35 對 / 36 反) | 主客看 `home` / `away` **欄位**(378 場全對、0 場反),網址只是 slug。照 slug 推會得到「這份資料一半的主客是錯的」這種完全錯誤的結論 |
| **FotMob 盃賽的賽程端點沒有 PK 比數,而且帶 season 參數會回「最新那季」** | 足總盃/聯賽盃(id 132/133)的 `fixtures.allMatches[].status` 只有 `scoreStr`(PK 場是平手比分)與 `reason.short`(FT/AET/Pen);照著印會把 PK 決賽講成平手、冠軍不明。足總盃 2026-27 還沒發布時帶 `season=2026/2027` 回的是 2025/2026 —— 不驗就把上季存成本季,畫面完全正常 | PK 比數在單場詳情 `header.status.reason.penalties`、輸家在 `whoLostOnPenalties`、打沒打延長看 `halfs.firstExtraHalfStarted`(三場真實比賽驗過);掛上去前核對隊 id 與最終比分。抓取器驗 `details.selectedSeason === 要的那季`。兩輪探測都是 `probe-fotmob-cups.mjs` 跑出來的,不是文件 |
| **抓取器的輸入靠一個「沒進工作流的手動步驟」,主來源一停就整條斷** | 西甲賽後報告停在 30/41 場:寫 `match-details` 的抓取器要從 `{季}-lineups.json` 拿 matchId,而那份靠 `laliga:lineups` **手動**跑(本機優先模式的遺物),停在 8/30。SportMonks 還在時它供 30 場、看不出來;9/3 退訂後新場次一場都進不來,而每次部署那一步都印「待補 0 場」—— **看起來完全正常** | 抓取器的輸入只能是「排程也會更新的東西」。這裡改讀 `game:fetch` 每次部署都在抓的逐場資料(41/41 場都有 matchId)。找「為什麼沒更新」時,先問**它的輸入是誰更新的**,再問來源通不通 |
| **同一個聯賽兩種 xG 算法,而且沒人講** | 逐場資料同時給球隊統計 xG 與逐射門 xG,逐場比對 41 場有 5 場差到 0.37(LEV\|BET 2.76 vs 3.13)。補進來的 11 場走球隊統計、既有 30 場走逐射門加總 → 同一頁兩種算法,而文章只對其中一種說明出處 | 一個聯賽一種算法。選有完整性檢查的那一條(逐射門加總要 `shotmapComplete`,進球數對得回比分),另一種在它不可信時才用。**新來源補進舊資料集時,要對齊的不只是欄位名,還有算法** |
| **拿「有沒有踢 PK」當兩個來源的紅線,會被缺列的那一邊擋掉整季** | 盃賽改接 FotMob 後跟 SportMonks 舊快取逐場核對,足總盃 2025-26 整季 123 場因為**一場**不一致被擋:Newport County 2-2 Gillingham,SM 記成延長後 2-2、沒有 PENALTY_SHOOTOUT 列;FotMob 賽程說 Pen、單場詳情有整輪 10 球的 PK。缺列的是舊快取,不是新來源 | 紅線只放在**兩邊都明講的東西**(最終比分)上;「有沒有 PK」這種一邊可能根本沒記的欄位,記成 `pensMismatch` 印出來、畫面講,不擋。跟「無法核對 ≠ 不一致」是同一個道理 |
| **兩份名單對照只比全名會整隊漏掉** | football-data 的 Inter 全名是 `FC Internazionale Milano`、Brest 是 `Stade Brestois 29`,跟另一邊的 `Inter` / `Brest` 一個共同 token 都沒有 | 全名與 **shortName 都要比**,取最好的一組。漏掉的那一隊會變成 10 場「隊名對不上」,交叉核對整份不通過 —— 看起來像資料錯,其實是對照表漏了 |
| **隊名正規化去掉字首的 AFC** | `AFC Liverpool`(第九級)被對成 `Liverpool FC`、`Bournemouth FC`(第九級)被對成 `AFC Bournemouth` —— 兩支第九級球隊在盃賽頁被標成「英超」,而畫面看起來完全正常 | **字尾**的 FC/AFC 是法人形式(Barnsley = Barnsley FC),**字首**的 AFC 是球隊身分的一部分。只去字尾。這是「盃賽寬鬆比對會對錯球隊」的第二次出現,靠 nearMisses 清單抓到的 —— **那份清單不是裝飾** |
| **同一組對戰在不同賽季會重複** | 用 `find(home===H && away===A)` 找比賽,拿到的可能是**上一季**那一場。西甲三則賽報因此全被判成「比分不符」:摘要寫 1-2、抓到上季的 0-2 | 用日期收斂(報導日或比賽日前後幾天內)。**收斂不到要記成「無法核對」,不是「不一致」** —— 那兩個結論差很多 |
| **人工交付的明細與賽程涵蓋的比賽不同批** | 新賽果落地、逐球明細還沒跟上時,「逐隊進失球對回賽果」整條變紅,看起來像上游資料錯了 | 明細只收核對通過的場次,所以 `goals.json` 每季記 `matchKeys`(涵蓋哪幾場),核對限定在同一批比賽上 |
| API 回 **HTTP 200 加一個 error 物件** | 只看 `res.ok` 會把失敗當成功;排程每天跑、每天回報成功,實際一筆都沒抓到 | API-Football 看 `j.errors`、Understat 看 `j.error`。而且要分得出「暫時失敗」與「這個方案就是拿不到」,後者要記錄下來讓畫面講實話 |
| **備援來源的方案限制被當成整季的結論** | 西甲賽後已由主要來源(SportMonks)發布 16/20,剛完賽那 4 場的頁面卻寫著「本站使用的資料源方案不含本賽季…在換成涵蓋本賽季的方案之前都不會出現」—— 而隔壁 16 場的球隊統計、正式陣容、事件與評分全都在。判斷式是「還有場次沒發布 → 把備援的 blocked 傳到前端」,主要來源換成 SportMonks 之後沒跟著改 | 「拿不到」只能在**主要來源一場都發不出來**時講。分成兩個欄位:`blocked`(整季拿不到,reportCount===0 才給)與 `backupBlocked`(主要來源可用、備援補不了缺口),缺口照實說是「還沒抓到」。`npm test` 有四條守著,含「主要來源已發布場次時 blocked 必須是 null」 |
| **比分的分段欄位不是累計值** | 歐冠的 `et` 是**延長賽的增量**,不是「延長賽後的比分」。2025-26 決賽 `final=[1,1]`、`ft90=[1,1]`、`et=[0,0]`、`pens=[4,3]` —— 照 `et` 印會變成「0:0(PK 4:3)」,而半場還印著 0:1,自己跟自己矛盾。這跟 `fullTime` 那一列是同一類錯:憑印象讀欄位 | **比分只讀 `final`。** 拿全部資料驗過:`final === ft90 + et` 在歐冠 378 場全部成立,而 `ft90` 有 372 場是 null。盃賽 1,444 場有 2 場上游自己加不回來 —— 那種要標「分段對不起來」,不挑一個當答案 |
| **RSS 來源的名字不等於它的內容** | `feeds.json` 裡叫「Sky Sports 英超」的那一筆,網址其實是 `feeds.skynews.com/feeds/rss/sports.xml` —— Sky News 的**綜合體育**。抓回來的拳擊與網球會掛在動態頁上標成「英超外電」,而畫面看起來完全正常 | 加來源時**先抓一次看標題**,不要只看名字。正確的是 `skysports.com/rss/11661`(2026-08-28 實測 20 則全是英超)。來源自己歸錯類的不算(Guardian 把週報測驗放在英超 feed 裡),那種不要用關鍵字黑名單去殺,會誤傷真文章 |
| **「姓氏唯一就配對」會對到完全不同的人** | 租借核對器的姓名 fallback 寫成「姓氏相同且唯一就回傳」,完全沒檢查名字 —— `Gustavo Nunes` 配到 `Matheus Nunes`(FPL 唯一姓 Nunes 的人,2861 分鐘)、`Fer López` 配到 `Hugo Bueno López`(2359 分鐘),然後核對器拿那些分鐘去**指控真紀錄是假的** | 姓氏相同**且名字首字母相同且唯一**才算配上,否則回 null。`npm test` 有三條守著(含 Gustavo ≠ Matheus)。這條坑本站講過很多次(「對錯人比對不到糟得多」),而核對器自己犯了 |
| **修好一份、忘了另一份複本** | 姓名配對在 `verify-loans.mjs` 與 `lib/loans.mjs` 各有一份。我修好核對器那一份,另一份沒動 —— **實際掛到球員身上的那一步仍在對錯人**,而畫面上完全看不出來。實測 20 筆掛錯:Ben Nelson 的租借掛到 Reiss Nelson、Julián Araujo 的掛到 Ronald Araújo、Gustavo Nunes 的掛到 Matheus Nunes | 抽成 `scripts/lib/names.mjs`,兩邊 import 同一份。`npm test` 有四條守著「兩個檔案都走共用的、而且沒有自己再寫一份正規化」。這條坑 CLAUDE.md 本來就寫過(「複製一份轉換過去的話,改了一邊另一邊會悄悄過期」),而我自己犯了 |
| **`npm test` 夾在 `laliga:build` 與 `build` 中間** | 資產戳是 `build` 最後一步(`stamp-assets`)寫的。測試卡在中間跑的話,es1 的 `meta.json` 剛被 `laliga:build` 重寫、戳還沒補回來 —— 「meta 記的戳跟實際檔案一致」**每次 CI 都假紅**,而 `continue-on-error: true` 讓沒有人去看 | 測試要驗的是**最終狀態**,所以排在 `build` 之後。2026-08-28 修正 `epl-live.yml`。這條在本機重現得出來:跑 `laliga:build` 之後直接 `npm test` 就會紅。**2026-09-09 同一個根因的第二次**:`laliga-matchday.yml` 的迴圈只跑兩支聯賽 build 就回寫,倉庫裡 es1 / en2 的 meta 從比賽夜到下一次部署之間都**沒有戳**(不是對不上,是 undefined),CI 因為永遠在 build 之後才測所以三個月全綠。迴圈補一行 `npm run stamp`;順帶發現守這條的清單**漏了英冠**,新增聯賽時要跟著加 |
| **0 是一個看起來很像答案的數字** | `check-docs` 用「算不出實際值就略過」來處理資料還沒產生的情況,但判斷只看 `null`。CI 的 checkout **有** `vault/` 這個目錄(因為 `vault/我的筆記/讀我.md` 進了版控,產物是 gitignore 的),於是算出 **0** 而不是 null,拿去比對文件寫的 5,716 → 假紅 | 「沒有資料」與「資料是 0」要分開。現在先看產生的子資料夾在不在,不在就回 `null`。凡是「算不出來就略過」的地方都要檢查這一點 |
| **`"score":[0,0]` 不是「沒有比分」** | openfootball 對 0:0 收場寫成陣列 `{"score":[0,0]}`,其餘寫成 `{"score":{"ft":[...],"ht":[...]}}`。只認 `score.ft` 去數,英冠 2025-26 會數成「552 場只有 518 場有比分」,於是斷定上游缺了 34 場 —— 我不但誤判,還把那個錯的數字寫進三個檔案的註解 | adapter 的 `readScore` 本來就兩種都讀,是**我自己另外數了一次**。那 34 場拿 football-data.co.uk 逐場核對,全部真的是 0:0。教訓跟 Understat 那條一樣:**「我數不出來」跟「上游沒有」是兩件事**,斷言缺漏之前先確認自己讀的是不是對的欄位 |
| **「主隊\|客隊」當鍵,在有附加賽的聯賽裡不唯一** | 英冠季末的升級附加賽由聯賽裡的四隊互打,2023-24 出現附加賽 `NOR\|LEE 0-0` 與聯賽 `NOR\|LEE 2-3` 撞同一個鍵 → 補比分的核對每季報 **5 場**「比分不符」、整份不採用。看起來像上游資料衝突,其實是自己的鍵撞了(5 = 準決賽 4 + 決賽 1) | 非聯賽場次要在**補比分之前**就標 `stage`(`leagueMatches` 的 `stageOf` 選項)。第一版是拿到 matches 之後才標的,補比分那一步根本看不到。另外加了一道:排除附加賽之後鍵仍重複就整份不做 |
| **產物的欄位名是跟前端的約定,不是自由發揮** | 加英冠時自己給 meta 取名字,**同一輪犯了四次**:`season`(前端讀 `currentSeason`)→ 畫面印「2025-26 完整・undefined 進行中」;`model.note`(前端讀 `caveats`)→ 整頁載入失敗;`sources[].label`(前端讀 `name`)→ 頁尾印「來源:undefined、undefined」;`reports.{pre,post}`(前端讀 `reports.reports[鍵]`)→ 首頁炸掉。**一個都不會拋錯到測試裡** | 新聯賽的產物欄位名一律照抄既有聯賽。`test-championship.mjs` 有一整節在守這件事(逐欄位跟 es1 的產物比對) |
| **「不是英超」不等於「就是西甲」** | 前端 `league()` 寫死 `=== 'es1' ? 'es1' : 'pl'` → `?league=en2` 靜靜退回英超,頁面正常顯示、只是顯示的是另一個聯賽的資料。vault 產生器兩處同病:`dataDir` 把非 pl 一律指向 `leagues/es1`、`clubDirectory` 非 pl 一律讀 `teams-la-liga.json` → vault 裡長出 `英冠/球隊/FC Barcelona.md`。`fetch-news.mjs` 是第四處:`--league=en2` 會靜靜抓成英超的 RSS 存進英超的檔案 | 判斷一律走註冊表(`LEAGUES` / `lg.key`),不要用「是不是某一個」的二元式。這種寫法在只有兩個聯賽時看起來完全正確。第五處(2026-09-05):分析頁用 `edition === 'basic'` 選精簡分支,英冠沒有 edition → 走英超完整分支 → 每一場已完賽的英冠單場頁都「載入失敗」 |
| **前端把聯賽的事實寫死** | `page-teams` 兩個分支都寫「20 支球隊」—— 英超西甲剛好都是 20,所以三年沒事;英冠 24 隊,照抄就在畫面上印一個假數字。同類的還有:sim 表的「前四」(英冠是前 2 直升、3~6 附加賽,第四名沒有意義)、`meta.edition === 'basic'` 被當成「是不是西甲」的二元旗標。**還有一種更難發現的:magic number 剛好小於一輪的場數** —— 實時戰況頁的「開賽倒數」寫 `upcoming.slice(0, 8)`,而一輪有 10 場,所以**每一輪都固定有兩場沒有倒數**,被切掉的還是開球最晚的那兩場(週日晚場與週一夜場)。實測 2026-27 第 2 輪:Man Utd vs Ipswich 與 Aston Villa vs Arsenal 在整區都找不到,而溢位連結寫的是「還有 362 場」(整季),讀者根本不會想到自己要找的那兩場就在裡面 | 數字一律從資料算(`teams.length`);分界線看資料有沒有那個欄位(`promotionPct`),不要在前端寫死聯賽代碼;文案由各聯賽的 build 寫進 `meta.intro` / `meta.boundaries` |
| **只顯示一部分的清單,要用「單位」當上限而不是筆數** | 修上面那條時試過三種,都量過整季每個時間點「把一輪切一半」的比例:固定筆數 22%/65%/69%(等於沒修)、湊滿一輪再停 0%/13%/0% 但卡片會漲到 19~23 張(其中十張是七天後的事)。**加「第幾輪」的標題救不了固定筆數** —— 只會讓不完整的那一組看起來完整,讀者找不到自己的隊會以為沒有比賽 | 最後用的規則沒有任何 magic number:**開球順序上第一段連續同輪**,卡片上限自然等於一輪的場數,剩下的收成一行摘要(下一批哪一輪、幾號開始)。抽成 `core.js` 的 `countdownFixtures` 才測得到 —— **測試看不到 DOM,留在頁面裡就只能用正則掃原始碼**,那種測試守不住邏輯 |
| **拿小樣本的值去比「大樣本平均」的分布,極端分位太便宜** | 位移雷達的 10 級分第一版拿「上季各隊**全季平均**」當尺 —— 38 場的平均把波動抹平(射門/場全季平均散佈 9.3~15.7,10 場視窗 7.0~19.7),近 10 場隨便一波熱潮就頂穿整把尺,曼城六軸幾乎全 10(使用者抓到的) | 尺要用**同樣本大小**的分布:上季全部球隊的所有 10 場滾動視窗(20 隊 × 29 窗 = 580 段)。10 才代表「比上季任何一隊的任何一段 10 場都強」。分位比較的兩邊樣本大小不同時,先想清楚哪邊的變異數被壓縮了 |
| **FPL 會把被罰下者的黃牌吞掉** | 拿官方事件流跟 FPL 對牌數,BHA vs AVL 官方是 5 黃 + 1 紅、FPL 是 4 黃 + 1 紅。少的正是 Gomes 自己 9 分鐘那張 —— FPL 記他「黃 0 紅 1」。**每有一個人被罰下,兩邊的黃牌數就差 1** | 那不是資料錯,是兩邊記法不同。要交叉核對牌數時先把被罰下的人扣掉。反過來說,這個差異本身是**核對紅牌代碼的好工具**:`R` 就是這樣確認的(官方 40 分 R 給 AVL,FPL 說 AVL 的 Gomes 紅牌 1、上場 39 分) |
| **官方事件流沒有「誰換誰」** | 換人是 `S` 型別、`description` 是 `ON`/`OFF` 兩筆分開的事件,**沒有任何欄位把它們連起來**。而同一分鐘同一隊可以換兩人:實測 FUL vs CHE 第 65 分,四筆事件的 `time.millis` 完全相同。照相鄰順序配對就是猜 | 不配對。按「分鐘 + 隊伍」分組,上場與下場各自列出,不宣稱誰替誰 —— 配錯人比不配對糟得多(租借姓名那條坑的同一個道理)。`npm test` 有一條守著「換人沒有誰替誰的欄位」 |
| **「有陣容就跳過」讓比賽中的事件永遠不更新** | `fetch-official` 原本的條件是「已快取 + 未完賽 + 有陣容 → 跳過」,而陣容**賽前一小時**就有了 —— 於是整場比賽都不會再抓,進球、牌與換人要等完賽才一次補上。比賽日的迴圈每 2 分鐘叫它一次就是為了拿這些,跳過等於那一步白跑 | 進行中(`status === 'L'`)的一律重抓。最多 10 場、一次 10 個請求,跟迴圈本來就在打的是同一個端點 |
| **用「還沒抓的資料」決定要不要去抓** | `live-window.mjs` 判斷「現在有沒有比賽在踢」時,只要 `data/raw/live.json` 讀得到就信它 —— 而那是**上一次抓的快照**,進場前根本還沒抓。實測:Crystal Palace vs Man City 開賽 10 分鐘,而快照是 **199 分鐘前**的第 1 輪(全部 finished),於是 `liveNow=0`、`fromFeed=true`,依開賽時間判斷那條被整個跳過,回報「不進場,下一場還有 979 分鐘」。**連手動觸發 `epl-matchday.yml` 都進不去**,整個高頻更新形同失效 | 信 feed 的前提是**它夠新**:`fetchedAt` 在 10 分鐘內才採用,否則退回用開賽時間推。輪詢迴圈裡信 feed 仍然是對的(那裡剛抓完,而且 `started`/`finished` 比 `fixtures.json` 的 `played` 快一拍)——差別只在新舊,所以用時間分。判斷抽成 `decideWindow({now, fixtures, live})`,`npm test` 有八條守著,含「過期就退回開賽時間」與「最後一場剛結束要收工」兩個相反方向 |
| **`upcoming` 不等於「還沒踢的比賽」** | `scheduleState` 對沒有 `kickoff` 的場次回 `unknown`,所以它們不在 `upcoming` 裡。而上游是**逐月公布開球時間**的:實測西甲 339/380、英冠 264/552 目前只有日期沒有時間。拿 `upcoming.length` 去講「本季還有 N 場」,西甲會顯示 **11 場**,而它其實還有 360 場 | 「還有幾場」一律數 `!f.played`。同理不要報「這一輪還有幾場」—— 同一輪可能只有一部分公布了時間,報出來是假的。倒數本身不顯示它們是對的(倒數不到一個未知的時間),但**數量不能跟著少** |
| **「等待賽果」把延賽講成資料落後** | 那一區的說明寫「時間上早該結束,但**資料源還沒更新比分**」。延賽的場次也會掉進這一區(開賽 115 分鐘後),於是畫面告訴讀者「比分還沒更新」,而那場**根本沒踢** —— 讀者會等一個永遠不會來的比分。一季有 2~7 場改期(英超 2023-24 六次、英冠 2025-26 七次),這個空窗一定會出現 | 本站沒有延賽的資料來源,**分不出是哪一種就兩種都講**,並且把「早該結束多久」給讀者自己判斷。這是鐵則四:不確定性要寫在畫面上 |
| **積分榜是從比分算的,不含扣分** | 英冠幾乎每季都有扣分處分(財政違規等)。2025-26 我們算出來的後三名是 BLB、OXF、SHW,**實際降級的是 LEI、OXF、SHW** —— LEI 與 BLB 同 52 分而 LEI 淨勝球更好,照比分算 LEI 是第 21 名 | 沒有扣分的資料來源就**不要猜一個數字填上**。做法是每次 build 自動對帳(我們的後三名 vs 實際離開聯賽的隊伍),對不上就寫進 `meta.tableCaveat` 並顯示在首頁的「目前資料界線」 |
| **Obsidian 的檔名跨聯賽會撞** | 升降級的球隊本來就該在兩個聯賽的名冊裡(Burnley 有英超歷史、本季在英冠),於是 `英超/球隊/Burnley.md` 與 `英冠/球隊/Burnley.md` 同名 —— 而 Obsidian 的 `[[連結]]` 是**跨資料夾用檔名解析**的,兩個同名檔會讓連結指到其中一個,不報錯 | 檔名集中在 `teamFileByCode` 決定,撞到的加聯賽後綴(`Burnley(英冠)`),順序照 `LEAGUES` 讓英超保持原名(改名會斷掉手寫筆記裡的連結)。兩則筆記互相加一行連結,讀者才知道另一則存在 |
| **把會隨資料變動的數字當 CI 紅線** | `docs:check` 第一版把 vault 筆記數、球員頭貼、傷停天數全都當紅線 —— 但那三個每跑一次資料就會變(vault 本機 5,716、CI 5,733;頭貼每次 `photos` 抓到新圖就 +1;傷停每天 +1)。結果是 CI 每次都紅,而紅久了就沒有人看 —— **比不檢查更糟** | 分成兩種:`drifts: true` 只回報不擋(文件裡那個數字標成「快照」),`drifts: false` 才是紅線(只有人為改動才會變,例如新交付、接了新資料源)。兩種都把實際值印出來,CI 的 log 才能自己說明 |
| **自動更新敘事裡的數字 = 竄改紀錄** | `docs:check` 守了「核對後**發布 594 筆**」這句 —— 但那是**收件當下的紀錄**,不是現況。CI 上實際算出 597(`fetch --force` 更新了 FPL 的 CSV,核對器的自我檢查放行之後多 3 筆可核對),於是 `--fix` 會把那句改成「當時發布了 597 筆」,而那是假的 | **檢查的對象只能是以「現況」呈現的數字**(現況表、「目前 N 場」)。敘事裡描述過去事件的數字是紀錄不是狀態,一律不碰。那一項已經拿掉,`check-docs.mjs` 裡留了註解說明不要加回來 |
| **`continue-on-error` 把「這件事做不了」藏成一行雜訊** | 補抓頭貼要用 Python 的 Pillow(輸出是 JPEG,而本專案零 npm 依賴、`lib/png.mjs` 只做 PNG),但 runner 沒裝。錯誤是抓第一個人時才丟的 `ModuleNotFoundError: No module named 'PIL'`,混在球員清單裡,而那一步是 `continue-on-error` —— **從第一次 CI 就一直紅,三次都沒有人發現** | 缺依賴要在**開跑前**檢查、講清楚、然後結束碼 0 離開(那不是「這一筆失敗」而是「這件事現在做不了」)。真正要修的是把依賴裝上:`epl-live.yml` 加了一步 `pip install pillow` |
| **上游欄位名不等於它的單位** | FotMob 的 `total_scoring_att` 直覺會當成「總射門數」,但交付檔的 `playerStatCategories` 自己宣告它是 **"Shots per 90"**;`total_tackle` / `interception` / `defensive_contributions` 也都是 per 90,而同一份裡的 `total_att_assist` 卻是總數。混著標就是編數字 | 欄位鍵用**上游的 slug**、把上游宣告的 title 一起輸出(`statMeta`),畫面上的中文標題要把單位寫進去(「射門(每 90 分)」)並附對照。交付檔自己講了單位就照它講,不要憑欄位名猜 |
| **NFD 分解不掉的字母會被整個刪掉** | `Đ` `Ø` `Ł` `ß` `ı` 都是獨立字元,沒有「基底 + 附加符號」可分解。`normalize('NFD')` 之後它們原封不動,再被 `[^a-z0-9]` 清掉 —— `Đorđe Petrović` 變成 `"or e petrovic"`。那比配不到更糟:剩下的殘骸有機會撞到別人 | `lib/names.mjs` 先逐字對照換掉再做 NFD。`Đ → Dj`(塞爾維亞/克羅埃西亞語的通用音譯,交付檔就是寫 Djordje),`Ð`(冰島 eth)是另一個字,照舊 → D |
| **拿 A 聯賽的資料去驗「租到 B 聯賽以外」的球員** | 檢查「租到西甲的人,西甲資料把他放在哪一隊」時沒先確認**目的地當季真的在西甲**。Racing Santander / Cádiz / Granada 2025-26 都在西乙,租過去的人本來就不會出現在西甲資料裡,而他離隊前在原隊留下的出賽紀錄被判成「矛盾」 | 只有目的地當季確實在那個聯賽時才做這項檢查。(第一份交付的 Pelayo Fernández → Cádiz 就是被這樣誤判的,而且已經發函退回過 —— 那次退回是錯的) |
| **拿 metadata 標籤當判決依據** | 交付檔把 Hull City / Southampton / West Ham 的 `parentLeague` 標成英超,但那幾季他們在英冠。原本這會讓整筆被退回 —— 可是那個欄位**下游根本沒用到**(接資料用的是隊碼),標錯不代表這筆租借是假的 | 標籤問題記成 `labelIssues` 回報,不退回紀錄。真正能定罪的是直接跟聲明衝突的證據:出賽分鐘、目的地球員名單、年份平移指紋 |
| **一筆日期解不開,整個來源的 15 則全沒了** | Sky Sports 的 pubDate 是 `Thu, 27 Aug 2026 19:00:00 **BST**`,`new Date()` 對它回 Invalid Date,而 `.toISOString()` 直接拋錯 —— 輸出只留一行「✗ Invalid time value」,那個來源整批消失 | `fetch-news.mjs` 的 `parseFeedDate()` 會把字尾的時區縮寫換成偏移量再解;還是解不開就用抓取當天並**把筆數報出來**(靜靜蓋上今天的日期會讓三天前的新聞排到最上面,而沒有人會發現) |
| **`data/raw/fpl/{季}-players.csv` 當季那一份裝的是上一季的數字** | 檔名寫著 2026-27,裡面 Raya 是 3330 分鐘 —— 跟 2025-26 那一份一模一樣,而 2026-27 只踢了 1 輪。拿它當本季分鐘用,會誣賴一批真資料(我用它核對租借,一口氣冤枉了四筆) | 用之前先自我檢查:**該檔的進球總和要等於 `goals.json` 記的該季總進球**。2024-25 是 1081 = 1081,可以用;當季那一份對不上,拒用。`verify-loans.mjs` 就是這樣擋的 |
| **人工交付的資料會整批複製、只改年份** | 2026-08-28 交付的租借檔,2024-25 整批是 2025-26 的複本(年份 -1)。17 組重覆裡 14 組「月日完全相同、剛好差一年」;而 Leeds United 2024-25 在英冠,檔案裡卻有 6 筆「2024-25 英超 / 母隊 Leeds」 | 逐季的**聯賽成員資格**(`results.json`)是最便宜也最硬的檢查:那支球隊當季在不在這個聯賽。再加逐季出賽分鐘與目的地聯賽的逐季球員。`npm run loans:verify` 三種都做 |
| **同一個物件裡兩個同名鍵,後面的靜靜蓋掉前面的** | 遊戲側寫本來就有 `tempo`(半場進球),我新加的跑動節奏也叫 `tempo` → 產物裡只剩半場進球,動畫拿不到節奏,畫面完全正常 | 加鍵之前先 grep 同一個物件有沒有同名;測試要驗**形狀**(那個鍵裡有 `distancePerMin`),不能只驗「鍵存在」。改叫 `pace` |
| **FotMob 熱區的鍵是 Opta id,不是 FotMob 球員 id** | `p168144` 對不上名單的 562727(Raya),一個都對不上,而且**不拋錯** —— 第一次跑 20 場「有 heat 0 場、錯誤 0 筆」 | `playerStats` 每筆有 `optaId`,用它對照。「對不上人」永遠是安靜的,收完要數一次配對率 |
| **迴圈裡的「還要不要跑」沒帶聯賽參數** | `laliga-matchday.yml` 迴圈裡呼叫 `live-window.mjs` 沒帶 `--league=es1`,預設看英超的窗口 —— 西甲比賽日可能提早收工或多跑,而 log 看起來一切正常 | 有聯賽參數的腳本,每一個呼叫點都要帶;2026-09-04 改成兩聯賽任一有比賽就繼續 |
| **社群賽果檔慢好幾天,畫面就一直「未賽」** | openfootball / football-data.co.uk 更新節奏是天,西甲 9/3、英冠 9/1–9/2 到 9/4 還沒有 | 第三來源 FotMob 賽程端點(一個請求整季),補的標「暫定」,重疊場次逐場核對,一場不符整份不用 |
| **`link()` 的 `league` 沒給就繼承目前聯賽,所以「英超寫成 null」不是英超** | 總覽頁把英超場次寫成 `league: lg === 'pl' ? null : lg`,想表達「英超」;站在西甲時 null 被當成「沒給」→ 繼承成 es1 → 英超的場次 id 拿去西甲賽程找,進去是另一場比賽、另外兩支隊。頁面完全正常,只是隊伍是錯的 | 明確給 `'pl'` 就是英超、不繼承(網址不帶 league,pl 是預設);跨聯賽的頁一律把聯賽寫出來,不要用「pl 就不給」。`npm test` 有一節釘住三種寫法 |
| **FotMob 烏龍球事件的 `team` 是踢進自家門的人那一隊,不是得分方** | 照 team 數進球,有烏龍球的場次逐隊進球數對不上比分,`goalEvidence` 判定事件不完整、射手整場退回逐人統計(而 FotMob 逐人 goals 常是 null)→ 射手全空 | 拿三聯賽全部資料驗過:有烏龍球的 85 場,翻轉後 85 場全對、不翻 0 場對。`toCanonicalDetail` 翻成得分方並標 `ownGoal` / `ownGoalBy`。跟 pulselive 那條(樣本 1、語意相反)不是同一回事,**兩家供應商語意不同**,別互相套用 |
| **上游時差寫成 CI 紅線,`npm test` 又刻意擋 deploy → 賽果整晚上不了站** | 「已完賽場次一律收盤價」這條在 IPS vs LIV 剛踢完就紅(football-data.co.uk 的賽季檔隔天才補到這場),21:26 收工部署與 03:23 排程都被擋,站上停在 17:41 的建置 12 小時。比賽日迴圈整晚正常、raw 裡終場比分都在,**畫面就是沒有**。notify 有開 Issue #1,但沒有人收到 | 斷言只能守**本站自己算的東西**;上游還沒到的資料要用旗標講實話(`closingPending`),不是當錯。`pickMarket` 三支 build 共用。另外,CI 紅了要**主動通知**(點火器 `ALERT_WEBHOOK` 或 GitHub 通知),不然 Issue 開了等於沒開 |
| **同一頁上兩個賽事,只有一個接了即時路徑;而預設顯示的是沒接的那個** | 9/8 聯賽盃之夜的隔天早上,使用者回報「目前沒有盃賽賽果」。實際上聯賽盃第三輪五場的終場比分**都在畫面上** —— 只是盃賽頁預設開在歐冠,而歐冠只在 12 小時一次的部署裡抓(raw 最後是 16:11 抓的、六場都還 `SCHEDULED`,下一次 04:09),所以預設那一頁整整七小時印「已開賽・等待資料」。那晚迴圈其實有進場,但進場的理由是聯賽盃、進去之後沒有人去抓歐冠 | **加即時路徑要看「同一個畫面上還有誰」**,不是只看自己那一份資料。預設顯示哪一個一律由資料決定(`core.js` 的 `defaultCup`:進行中 → 最後完賽時間 → 下一場開賽時間 → 平手回第一個),不要寫死。順序可以是使用者指定的,預設**不行** —— 那是狀態不是偏好。還有一個坑在坑裡:第一版把規則寫成「離現在最近的一場」,而昨晚兩個賽事都有 19:00 UTC 的場次、差距一模一樣,平手退回第一個 = 原樣。**平手 case 要單獨寫一條測試**,只驗最終答案的話錯的版本也會過 |
| **「兩邊沒有共同 id」不等於「接不起來」** | 西甲的比賽事件與評分沒有頭貼,我第一輪的結論是「賽後報告用 FotMob id、球員檔用 Understat code + SportMonks id,沒有對照表,所以做不到」——**而球員檔裡就有 727 張頭貼**,是使用者反問「資料庫本來就有不是嗎」才回頭再查的 | 換一條鍵:**(當季 + 隊碼 + 背號)** 同一隊同一季唯一(當季 427 人裡 381 組唯一、只有 1 組撞號),接到 1,426 筆。**背號當鍵、姓名當守門** —— 其中 10 筆姓名對不起來,那是季中背號換人(`Diego Rico(OSA #15)` 會拿到 `Raul Moro` 的臉),一律不採用。兩個細節:球員檔含兩季,**不收斂到當季的話撞號會從 1 組變成 265 組**;事件只有 `playerId`,而它就是名單的 `providerId`(717/719)。還有一條界線:**點擊的身分與查頭貼的鍵是兩件事** —— 拿 FotMob id 當 `data-player-code`,點下去會找一個不存在的球員 |
| **遠端網址的頭貼會留下破圖框** | 西甲的頭貼是 SportMonks CDN 的**網址**(英超是內嵌 base64)。圖被下架、CDN 不通、讀者網路擋掉,都會在畫面上留一個破圖框 —— 而同一頁其他地方都好好的,**讀者會以為站壞了** | `error` **不會冒泡**,要用 capture 階段監聽才抓得到;一條涵蓋全站所有 `.pphoto`。有 `data-team` 的退回隊徽(尺寸不變、行高不跳),沒有的整個拿掉。順帶修好球員頁本來就有的那些破圖 |
| **第一次繪製之前 await 一個跨網域請求** | 盃賽比分快速通道的第一順位是 `raw.githubusercontent.com`,而它寫成 `applyCupsLive(cups, await fetchCupsLive(cups))` 放在畫面之前。連不通時瀏覽器**不會馬上放棄** ——實測 `ERR_CONNECTION_RESET` 花了 **12.9 秒**,那 13 秒畫面上只有「載入資料中…」,**而且不報錯**,看起來就像頁面壞掉。備援(本機那份)在那之前根本輪不到 | **先畫再覆蓋**:拿到而且有變才重畫。抓取一律給 `AbortSignal.timeout` —— 「連不通會很快失敗」是錯的直覺。`npm test` 守兩條:抓取有逾時、繪製的呼叫在覆蓋之前(直線腳本的原始碼順序就是執行順序) |
| **上游的欄位有時候是物件,有時候是字串** | FotMob 事件的 `comments` 有時是 `{ localizedKey, defaultText }`,`esc()` 直接吃就在畫面上印 **`[object Object]`** —— 三個聯賽合計 57 列,全是牌事件的原因。**不拋錯,只是印垃圾**,而 `npm test` 看不到版面,所以它在站上待了一段時間沒有人發現 | 會進畫面的上游欄位,取值前先看型別:物件取講得出來的那個鍵(這裡是 `defaultText`),取不到就**整句不印**,不要印一句沒有意義的。發現它的方法只有一個:**改完真的把頁面開起來看** |
| **只讀了資料的其中一個區塊,而少掉的那些剛好是最有資訊的** | 歐冠跨聯賽評分的橋,第一版只讀 `leagueMatches`(聯賽階段 144 場/季),沒讀 `rounds[].ties[].legs[]`(淘汰賽 45 場/季)。橋從 219 掉到 152,回測改善從 0.0149 ± 0.0061(**通過**)變成 0.0125 ± 0.0073(**沒通過**)—— 於是「有證據才給預測」那道閘把預測全部關掉,而畫面完全正常、一個錯都不報 | 上游把同一種東西放在兩個地方時,**走整份收一次,不列舉區塊**(`uclSeasonMatches` 用 id 去重的走訪式,跟 `uclClubs`、`uclTeamAssets` 同一個寫法)。列舉的話以後多一個區塊就會有一批資料靜靜掉隊。`npm test` 守著「橋 ≥ 200」—— 只驗「有橋」的話 152 也是綠的 |
| **註解裡的 Markdown 掉進 HTML 字串** | 本專案的註解大量用 `**強調**`,而前端**沒有任何 Markdown 處理器** —— 同樣的寫法寫進 innerHTML 的樣板字串就在畫面上印出兩顆星號。不拋錯、不影響功能,而 `npm test` 看不到版面。實測掃出 5 處,**其中 3 處是既有的**(盃賽輪次說明、總覽的共識層說明、歐冠賽程說明),已經在站上一段時間 | 進畫面的字串用 `<b>`;`title` 屬性裡連 `<b>` 都不能有,直接寫純文字。`npm test` 加了一條:先把註解剝掉再掃 `**…**`(不剝的話整份都是誤報,而 `**` 也是次方運算子,要排除數字與空白開頭)。跟 `[object Object]` 那條同一類 —— **發現它的方法只有一個:改完真的把頁面開起來看** |
| **只看差值就宣稱有偏差,沒有算標準誤** | 診斷跨聯賽評分時看到「葡超被高估 0.34 分/場、英超被低估 0.34」,我當場寫下「模型系統性高估頂重聯賽」並準備加補正。**算了標準誤才發現**:葡超只有 37 場,SE 0.22,那是 1.5 SE —— 雜訊。七個聯賽裡只有英超到 2.4 SE,而**測七次,純靠運氣就有 0.28 的機率至少一個超過 2SE** | 逐組比較一定要帶 SE 與**多重比較**的代價。這條坑跟位移雷達那條(拿小樣本比大樣本平均)是同一家族:**分組之後每組的樣本都變小了**,而差值看起來不會變小。後續也印證了:照那個「偏差」調出來的補正,換一季驗收只有 0.0009 ± 0.0079,兩季的參數還互相矛盾 |
| **不是每個聯賽都是秋春制** | 接歐冠缺的那十六個聯賽時,賽季字串一律組成 `2026/2027` —— 而挪威超與哈薩克超是**春秋制**(三月開打、年底結束),FotMob 的字串是曆年 `2026`。那兩個聯賽本季**一份都沒抓到**,而症狀只是「`fotmob-ucl-leagues/no1/` 是個空目錄」加上兩支球隊沒有預測,一個錯都不報 | 曆年制在對照表標 `calendarYear: true`,賽季字串照它組。**證據不是憑印象**:同一份探測裡九月時挪威已完賽 152/240、哈薩克 190/240,而秋春制那些只有 20~54 —— 進度本身就說明賽季早就開打了。另外加一道:對照表裡有、卻一份快取都沒有的聯賽要記進產物的 `missingLeagues`,不然它只是安靜地從清單消失 |
| **同一份資料的兩個複本由兩條工作流寫,時間戳當紅線就會在比賽夜每次紅** | 「英超與西甲的 `ucl.json` 完全相同」這條在 2026-09-09 比賽夜紅了 —— 兩份**其餘 538,455 位元組完全相同**,只有 `retrievedAt` 差 9 分鐘(pl 17:51:06、es1 17:41:54)。原因是這兩個檔由 `epl-live` 與 `laliga-matchday` 各自寫,比賽夜兩邊交錯推送。看起來像「有人複製了一份轉換邏輯」,實際上資料一模一樣 | 紅線只放在**資料**上,比對時把時間戳排除。時間戳差太多仍然要講,但**只回報不擋**(差 > 60 分鐘印一行)—— 跟 `docs:check` 的 drifts / 紅線同一個分法。這是 `cups.json` / `cups-live.json` 那一課的第二次:**斷言只能守本站自己算的東西,不要守「誰先寫」** |
| **同一份上游 payload 裡,`matches` 與 `standings` 不同時更新** | football-data 的歐冠回應同時帶賽果與官方積分榜,但**積分榜晚幾小時**。2026-09-10 比賽夜實測:比賽區已有 12 場完賽、積分榜只算了 6 場,於是剛 5-1 贏球的 Barça 官方仍是 0 分 0 進球。逐隊核對就報成「積分不符」,`npm test` 紅、部署被擋 —— 而兩邊都沒錯,只是**涵蓋的比賽不同批** | 先比**場次數**:不一樣的記成 `pending`(只回報、畫面講),**只有場次數相同卻積分/進失球對不上才是 `mismatches`**,那才是真的有人算錯。畫面要講出「官方榜還沒算進最新場次」,不然讀者看到一張自己跟賽果矛盾的表(鐵則四)。這是「上游時差寫成 CI 紅線」的第二次 |
| **兩個配置能預測的場次不同,直接比平均會把「多預測了幾場好預測的比賽」算成模型變好** | 歐冠涵蓋率驗收:新配置 2025-26 的 RPS 是 0.2114、舊的是 0.2181,看起來準度大幅提升 —— 但新配置多預測了 77 場,而多出來的多半是「強隊對弱隊」這種好預測的。在**兩邊都有預測的 104 場**上成對比,真正的增益只有 0.0026 ± 0.0012 | 比較一律在**共同場次**上做成對比較(`paired`)。涵蓋率變了就不能比平均 —— 這跟「我的預測」那條「比較一律在同一批場次上做」是同一條規則,只是這次是自己的兩個配置在比。結論也要照實寫:**主要收穫是涵蓋率,不是準度** |
| **東西在但沒有按鈕**(「按鈕在但點了沒東西」的反面) | 歐冠預測涵蓋 116 場,而展開鈕的條件寫成 `comparable`(要兩隊都在 `ucl-standings` 裡,只涵蓋五個聯賽)—— **55 場有預測卻沒有按鈕**,讀者完全看不到,而且不報錯 | 展開鈕的條件是「**有東西可看**」,不是「有某一種東西可看」:`expandable = comparable \|\| 有預測`。而且**點擊委派的查表要用同一個條件** —— 兩邊不一致就變成真的「按鈕在但點了沒東西」。加了功能之後要回頭問:**它的入口是誰決定的?** |
| **同一份資料兩個渲染路徑,一個 esc 一個不 esc** | `model.caveats` 被首頁與模型頁各畫一次:首頁**直接插入**、模型頁走 `esc()`。所以寫 `<b>` 的話一頁粗體、另一頁印出 `<b>`;而實際上兩頁都在印星號 —— 英冠的 `intro`、`boundaries[5]`、`caveats` 裡各有一句 `**強調**`,而前端沒有 Markdown 處理器。**這是「註解裡的 Markdown」那條坑在資料裡的版本**,所以掃原始碼的那條測試看不到它 | 契約定成**純文字**,兩個渲染路徑都 esc() —— 寫 caveat 的人不必知道它會被畫在哪一頁。`npm test` 掃**整份 meta**(不是只掃 caveats:第一版只掃 caveats,漏掉 intro 與 boundaries,而星號就在那兩個欄位裡)。查的方法是**把每一頁開起來抓 `**…**`**,不是讀程式 |
| **依設計拒收之後什麼都沒留下,於是一場上游爭議擋掉整站** | 補比分的規矩是「一場對不上就整份不採用」(對的)。但拒收後產物裡沒有任何紀錄,於是:讀者看到踢完的比賽還寫「未賽」而畫面不解釋;測試分不出「依設計拒收」與「補比分壞了」,`FotMob 已完賽的場次本站沒有一場還是未賽` 就會紅 —— **一場爭議 → 21 場暫定賽果全撤 → 部署被擋 → 整站凍結** | 拒收要進產物(`meta.model.scoreCheck.refused`),讓畫面講得出來、也讓測試分得出兩種情況:有拒收就只回報,沒拒收才是紅線(那才代表補比分該做而沒做)。跟 `closingPending` 同一套。**設計成「整批不採用」的地方,都要問一次:撤掉之後誰看得出來?** |
| **交付檔的一個區塊整批標錯隊** | 6 筆「→ Alaves」裡有 5 筆被 Understat 證明其實在 Getafe,而同一份檔案裡「→ Getafe」的引進是 0 筆 | 同一個(賽季 + 目的地)是一個區塊。**區塊裡只要有紀錄被證明是錯的,剩下的整批不採用** —— 挑通過的用等於在兩個對不上的來源裡選一個喜歡的答案(進球明細那次的教訓) |
| **加了第二個東西,而「只有一個」那句寫死在畫面上** | 階段 C 讓站上有了**兩個模型**(域內 + 跨聯賽),而模型驗證頁提到歐冠 **0 次**,最後一段還印著「盃賽與洲際賽事…**本站目前只做聯賽**」——歐冠頁掛著 116 場預測,讀者在唯一該講出處的那一頁查不到憑什麼,而那句話已經是假的。不拋錯、不影響功能,`npm test` 也看不到版面 | 這是「前端把聯賽的事實寫死」的親戚:**寫死的不是數字,是「我們只做 X」這種宣稱**。加了一個能力之後要回頭問:**有哪一頁還在講我們沒有它?** 補的那一節數字一個都不准寫死,產物多開欄位讓前端讀(`results` / `reason` / `rejected` / `poolTrials`)——前端自己抄一份的話,改了 lib 那邊會悄悄過期 |
| **測試掃原始碼,而自己的註解就是誤報來源** | 新加「模型頁不再寫『本站目前只做聯賽』」那條,第一版掃整份原始碼 → **我自己寫的註解裡就在講那句話** → 永遠紅,而紅的原因跟它想守的事一點關係都沒有 |先剝註解再掃(`**強調**` 那條掃描器就是這樣)。凡是「掃原始碼找某個字串」的測試,**寫測試的同一輪一定會在註解裡提到那個字串** —— 這不是偶然,是必然 |
| **把「目標達成」寫成 CI 紅線** | 歐冠涵蓋的斷言寫成 `ratedTeams < totalTeams`(**一定要有球隊沒評分**)。9/10 頁面上寫「Fenerbahçe 與 Sabah FK 踢完第一場就會有評分」,**一個晚上就成真**(34/36 → 36/36、預測 116 → 126 場)—— 然後 `npm test` 紅在「補齊了」。而它**下面那一段註解本來就在警告這件事**,寫註解時看著上一行卻沒改 | 守的要是**計數自己不矛盾**(評分數 ≤ 球隊數),涵蓋率印成一行只回報不擋。凡是寫「等某某補齊」的斷言,一律先問:**補齊的那一天,這條會變成什麼?** 發現它的方法不是讀程式,是現實把預測兌現了而斷言擋在那裡 |
| **可重用 workflow 的 `inputs.description` 裡寫了運算式語法 → 整支 workflow 消失** | 拿 `description: '…(填 ${{ toJSON(needs) }})…'` 當說明,GitHub 把它當成**要求值的運算式**,而 `needs` 在 `workflow_call` 的 inputs 這一層根本不存在 → **整份 parse 失敗**。症狀不是「通知壞掉」而是**四支 workflow 全部跑不起來**:手動觸發直接被拒、排程不會開火。本機 YAML 語法檢查全過、`npm test` 全綠 —— 擋它的是 GitHub 自己的運算式層 | 描述只寫純文字。而唯一能發現它的方法是**改完真的去按一次觸發** —— 跟「改前端一定要真的開來看」同一條:本機驗證看不到的那一層,一定要去那一層驗一次 |
| **幾支 workflow 共用一個通知標籤,會互相關掉對方的 Issue** | 把 `epl-live.yml` 的 notify 抽成可重用的給四支用,第一版沿用單一 `ci-failure` 標籤 —— 比賽日那支還紅著,每日外電那支跑成功就把它的 Issue 關了,而 Actions 頁看起來一切正常 | 標籤帶呼叫端識別字(`ci-failure` + `ci:<key>`),要一次看全部就篩前者。另外兩條:判定只看呼叫端傳進來的 `toJSON(needs)`(GitHub 自己算的),「哪一步紅的」那段包在 `try` 裡 —— **它把整個通知帶走的話,CI 紅了反而更沒有人知道**;手動觸發的探測 workflow **刻意不掛**,失敗本來就是它要回答的問題,開 Issue 只是雜訊 |
| **grid item 的 `min-width:auto` 被撐開,而修在錯的那一層** | 球員頁在 400px 視窗 body 橫捲到 718:逐場紀錄表(min-content 約 700)把整條 grid 欄撐到 703,於是**每一張卡片**都 704 寬,連最上面只有名字那張也是。第一版把 `min-width:0` 寫在 `.player-page > .card` 上 —— 沒效,因為撐開它的是 `#playerLog`,一個沒有 class 的 div | 量 `grid-template-columns` 的算出值,不要猜是哪張卡片;規則下在 `> *`。逐場表本來就包在 overflow-x:auto 裡,但那只在**外層肯縮**時才有用 |
| **掃描把兩種假象當成 bug** | 全站掃描第一版報 allplayers 破圖 37 張、es1 球員頁 console 錯誤 30 次。追下去:32 張是 `loading="lazy"` 在首屏外**還沒開始載**(`complete=true`、`naturalWidth=0`,跟破圖一模一樣);console 錯誤全是 `ERR_TUNNEL_CONNECTION_FAILED` —— 沙箱擋外部圖片,正式站不會 | 破圖只數視窗內的;外部資源載不到另外計,不混進 pageerror。**量測工具自己也會騙人,第一次的數字要追到原因再信** |
| **同一句文案寫死「這是英超」,而它被三個聯賽共用** | `core.js` 陣容卡的來源說明看 `advanced.source`:不是 fotmob 就印 SportMonks / API-Football,是 fotmob 就印「**英超官方公布的正式名單**」。英冠與西甲的 FotMob 場次走同一張卡,於是英冠單場頁印著英超官方 —— 不拋錯,`npm test` 看不到版面 | 看**報告是哪條路建的**(`m.source`:供應商路徑有值、英超的 FPL + pulselive 路徑沒有),不看 advanced 是哪家。「前端把聯賽的事實寫死」的親戚:寫死的不是數字,是「這是英超」。共用元件裡任何提到聯賽名或官方機構的句子,都要問一次:**別的聯賽走到這裡會印什麼?** |
| **「只有一個」那句寫死在畫面上,第二次 —— 而且就在加了那個東西的那一頁** | 歐冠頁底下一整段「**這一頁沒有勝率預測,這是刻意的**」,階段 C 之後 126 場有賽前勝率,那段還掛了三天。修模型頁那次(上一列的那條)只回頭查了模型頁,沒查歐冠頁自己 | 加了能力之後要回頭 grep 的不只是「別的頁」,**首先是同一頁**。這種句子一律改成從產物讀數字(`model.fixtures.length`),讓它在資料變的時候自己變,不靠人記得回來改 |
| **「只有一個」那句寫死在畫面上,第三次 —— 前兩次修的都是「想到的那一頁」,沒掃過的頁還有三句** | 修完模型頁與歐冠頁之後,盃賽頁**頁首**仍印「三個賽事都沒有勝率預測」(歐冠分頁下面就掛著 126 場勝率)、「我的預測」頁把歐冠整個當成 `noModel`(說明卡:「這個賽事沒有本站的勝率預測」)、總覽表尾寫「歐冠與盃賽場次沒有分析頁(模型是聯賽調的)」。三句都不報錯,`npm test` 也看不到 | 加了能力之後**用那個能力的關鍵詞全站 grep**(這次是「勝率預測」「沒有模型」「noModel」),不要逐頁想。每一句改成從產物讀(`ucl-elo.fixtures.length`),字面的否定句只准留在「資料真的是零」的分支裡。`npm test` 各釘一條 |
| **第一次繪製時量到的寬度不是最後的寬度** | 折疊式表格在畫完的 microtask 裡量表格寬度,而那時字型與隊徽還沒載入 —— 英超首頁賽程表在 1200px 量到「放得下」,資源載入後長成 1163 對 1158,差 5px 沒被收,頁面照樣正常只是多一條橫捲軸。溢出大的頁面(1000px)反而沒事,因為第一次就量得出來 | 量版面的東西要在**尺寸真的變了**時再量一次(ResizeObserver 同時看容器與表格),但重算條件要寫「還有得收」—— 不然收無可收的手機寬度會無限重跑。實測的方法不是讀程式:把幾個寬度都開一次,看「fits」那一欄 |
| **核對的層級跟第二來源「有什麼」不一致,整季紅字而資料其實沒問題** | 本季歐冠的第二來源是賽季前的 FotMob **抽籤檔**(144 場 kickoff 全是同一個佔位值、沒有比分),而 `loadUclSeasons` 對每一份檔都跑**逐場**核對(日期 + 主客 + 比分)→ 138 場「對照來源沒有這一場」,歐冠頁整季印「⚠ 第二來源核對沒過」。講的是真話,讀者卻會以為賽果有問題;而它在站上掛了兩週 | 先看第二來源**有哪些欄位**再決定核對什麼:抽籤檔只核配對與主客方向(`crossCheckDraw`),有比分的檔才逐場核。產物帶 `kind`,畫面依 kind 講話。而且**抽籤檔永遠不能替球員榜背書**(它沒有比分可核)。跟「上游時差寫成紅線」同一家族:紅字之前先問,對不上的那一層,第二來源到底有沒有 |
| **「主隊\|客隊」當鍵,在歐冠一季會撞兩次(聯賽階段 + 淘汰賽)** | 英冠附加賽那條坑的歐冠版:2025-26 有 7 組同一主客在同一季踢兩次(Arsenal vs Atlético 聯賽階段一次、淘汰賽一次)。逐場抓取器用「主\|客」當快取鍵,第二場被當成**已快取**而跳過 —— 回填 189 場只進了 182,`attempts` 是 0,log 印「新增 182 場」,**一個錯都不報**。我還先寫了一條「missing 0」的檢查,因為鍵存在就算有 | 歐冠的配對鍵帶日期(`pair` = 主\|客\|日期,兩隊不可能同一天踢兩次),results 與 raw 都帶;`pairOf()` 有 pair 用 pair、沒有退回主\|客(三個聯賽不變)。舊 raw 在抓取器載入時就地改鍵。**而且同一個坑有兩層**:改完快取鍵之後,FotMob 賽程的查表(`byPair`)還是一個鍵一場、後寫的蓋掉先寫的 —— 補抓那 13 場全部「日期不一致」,因為查到的是同一組主客的另一場。查表一個鍵放一串,用日期挑。**任何一季會重複對戰的賽事,主客不是鍵** —— 盃賽、歐戰、附加賽都是;而「鍵」不只一個地方 |
| **FotMob 的 shotmap 把 PK 大戰的十二碼也列進去** | 2025-26 歐冠決賽 1-1(PK 4-3):shotmap 有 9 顆「進球」,其中 7 顆是分鐘 122~130 的 Penalty —— 互射十二碼。照算:射門圖判定不完整(9 ≠ 2)、xG 多加七次、射門圖上多七個實心圓。**第一版用「分鐘 > 120」排**,而 2024-25 Atleti vs Real 的互射從 **120** 起跳 —— 正規延長賽的最後一分鐘也是 120,光看分鐘分不開 | `isShootoutShot(s, { pens })`:`period === 'PenaltyShootout'`,舊 raw 沒 period 就看**這一場有沒有踢 PK**(results 的 `pens`):有,分鐘 ≥ 120 的 Penalty 一律當互射;沒有,一顆都不排。pens 帶到 detail 與報告,前端射門圖用同一條。射門存 `period`。三個聯賽沒有 PK 大戰,行為不變;盃賽接射門圖時要記得這一條 |
| **只畫「下一輪」,踢完的那一輪就從畫面上消失** | 歐冠聯賽階段只畫一輪,預設是「還沒踢完的最小輪次」,而且**沒有輪次切換鈕**。第 1 輪 18 場 9/10 踢完,頁面直接跳到 10/13 的第 2 輪 —— 18 個終場比分都在資料裡、都不在畫面上,使用者 9/12 回報「完全看不到比完的比分」。註解還寫著「讀者要的是下一批什麼時候踢」| 「東西在但沒有按鈕」第三次。預設由**離現在最近**決定(`core.js` 的 `defaultMatchday`,跟 `defaultCup` 同一套:進行中 → 最近 → 平手時有賽果的贏),而且**每一輪都要有入口**。只顯示一部分的東西,一定要問:其他部分讀者怎麼到? |
| **第一次畫面用部署快照,第一次輪詢在 20 秒後** | 使用者在比賽中回報「重新整理比分回歸」:頁面載入先讀 Pages 上的 `live.json`(部署當下的快照,#437 是 14:19 抓的),迴圈推到 raw 的最新比分要等計時器的**第一個 tick**(20 秒)—— 重新整理的頭 20 秒比分退回部署時的狀態,離部署越久退得越多(當時差二十幾分鐘)。總覽更糟:沒有輪詢,而且「即將到來」那張表是算一次的字串常數,盃賽小檔覆蓋後 `render()` 畫的還是舊字串,**盃賽的即時比分在總覽從來沒出現過**,一個錯都不報 | 「先畫再覆蓋」是一整套,不是只有盃賽頁:畫完**立刻**拿一次 raw 那份(`overlayLive()`),再交給輪詢;抓取全站一份 `C.fetchFeed`(raw 優先、一律 4 秒逾時)、路徑一份 `C.liveFeeds`。會被覆蓋改到的表要在 `render()` 裡重算,不能是常數。`npm test` 守序列(先覆蓋一次再輪詢)、守共用(沒有人自己再寫 fetch 迴圈)、守總覽的表在 render 裡。**凡是「第一次畫面 + 之後輪詢」的頁,都要問:第一次畫的是哪一份、多舊?** |
| **「往目標插值」不是運動模型** | 模擬遊玩的跑位動畫寫 `p.x += (目標 - p.x) * k`,於是速度跟「離目標多遠」成正比、**完全沒有上限**:量出來尖峰 160 ~ 482 m/s(577 ~ 1735 km/h)、全隊均速 5.6 ~ 6.1 m/s 而且三種播放速度下都一樣 —— 畫面上二十二個人整場都在衝刺,偶爾瞬移。而逐人最高速度(FotMob 真資料)當時只當倍率,沒有真的當上限。使用者的回報是「動作跑動還不真實」 | 要有速度向量:目標速度依狀態(走/慢跑/跑/衝刺)、**上限是他自己的真實最高速度**、每格只能改變 `ACCEL × dt`(煞車更快),接近目標用 v² = 2ad 收速度。校準要有**外部對照組**:`pace.distancePerMin / 11`、`sprintDistPerMin`、`sprintsPerMin` 三個一起對(實測 115 對 114)。兩個陷阱:**幾何約束之後不能量跑動**(間距兜底一格推 1.6 m ÷ dt = 48 m/s,尖峰整個假掉),**定義不一樣的數字不能比**(FotMob 的衝刺要持續,數「每次跨過門檻」會多 3 倍,看起來像模型太衝) |
| **判斷「踢完了沒」的兩個欄位在剛結束時都還是舊的** | 使用者在比賽剛結束時回報「比賽途中其實就有資料、有比分結果,結束卻歸零、寫沒賽果」。`scheduleState` 只看賽程時間與 `fixture.played`,而 `played` 要等社群賽果檔(**天**為單位)、FPL 的 `finished` 要等加分算完(實測開賽後 118 分鐘還是 `started && !finished`)。於是 115 分鐘一到,場次從「進行中」掉進「還沒有賽果」,而那一區不讀 live.json —— **比分就在同一頁的資料裡,畫面上卻消失了**,還附一句「本站還沒拿到比分」 | 誰最新就信誰:`scheduleState(f, now, m)` 收即時快照,說完場就完場、說在踢就在踢(不受 115 分鐘限制)。但**信它的前提是它夠新**(`feedFresh`,15 分鐘)—— 靜態站上的 live.json 可能是上次部署那份。快照過期時 phase 不採用它,**但那個比分仍要印出來**並標「幾分鐘前抓的、官方賽果還沒到」,不要退回「賽果未取得」(鐵則四:不確定性寫在畫面上,不是把資料藏起來)。兩區都收同一場時記得去重。**同一個根因在兩頁上各有一個洞**:單場分析頁的即時面板也寫 `m.started && !m.finished`,完場就把比分、場上數據、陣型整塊清掉,而那時賽後分頁還沒被渲染 —— 整頁只剩賽前分析、大字是**賽前預期進球**。修這類坑之後要問:**還有誰用同一個判斷?** |
| **用「資料形狀」分岔時,挑到兩邊都有的欄位** | 退掉 `edition === 'basic'` 時,用 `p.team !== undefined` 分辨英超與西甲的球員資料 —— 而**西甲的球員檔也有 `team`**,於是西甲走了英超那條路,球隊頁在 `p.price.toFixed(1)` 整頁炸掉。同一輪還有第二個:戰術頁改成「去 fetch formation / shapes,缺就走另一套」,功能對,但西甲每次進這一頁都多打兩個 404 | 挑欄位之前**兩邊都數一次**(`players.some(p => p.last \|\| p.current)` 才是真的形狀差別:英超一人一列拆上季/本季,西甲一人一季一列)。而且**判斷要用手上已經有的資料,不要為了判斷再去要一次** —— 戰術頁改成問已載入的 `tactics`(英超每隊帶 `squad`)。兩個都是 `npm run sweep` 抓到的,不是讀程式看出來的 |
| **分類表會長,而快取凍住了抓取當下的分類** | 2026-09-12 出現第三種牌代碼 `YR`(兩黃罰下),當時表裡沒有它,於是快取寫下 `kind: null`。**把 YR 加進表也不會自己變** —— 快取不會重算,沙箱又重抓不到,而測試讀的是產物。同一件事還有第二層:時間軸的紅牌圖示寫死 `kindRaw === 'R'`,所以 `YR` 畫成小方塊 ▪、用黃牌的顏色,而那個人是被罰下的 | 分類**以原碼為準、在 build 時重算**(`CARD_KINDS[e.kindRaw] ?? e.kind`),新代碼核對放行後舊快取自動跟上;「會不會離場」由產物給欄位(`sendsOff`),**前端不要列代碼**。放行 `YR` 的核對法跟當初 `R` 同一套:同一人 28′ 已有 `Y`,而官方 5 黃 + 1 YR 對 FPL 4 黃 + 1 紅 —— 差的正是被罰下那人的黃牌 |
| **官方把補時全部記成第 90 分** | 補時第幾分只在 label 裡(`90+7'00`),`min` 一律是 90。照 `min` 排序,**90+7 的進球會排在 90+1 的黃牌前面** —— SUN vs ARS 實際長這樣,時間軸自己不按時間。這個 bug 在站上很久,是改換人排版時**把頁面開起來看**才發現的 | 排序與分組都用 `absMin(label, min)` 解析出來的實際分鐘。分組鍵也要改 —— 不然 90+1 與 90+7 的換人會被當成同一次換人 |
| **排序哨兵印在畫面上** | 表格欄位常寫 `value: x ?? -1`(null 排不動),但**沒配 render 就會把 -1 印出來**。西甲 VIL 與 OVI 的進球情境分類對不回整季總進球,產物依設計給 null → 戰術頁的角球/其他/直接任意球三欄印「-1」、進失球那欄印「null / null」。`npm run sweep` 只抓到後者 —— 它掃**字面的 null**,而 -1 它看不出來,因為那看起來完全像一個真數字 | 凡是用哨兵的欄位都要經過一層 `cell()`:沒有值印「—」,title 說出為什麼,區塊說明把受影響的球隊列出來(鐵則四)。`npm test` 掃 `?? -1,\s*num: true }` 這種「用了哨兵卻沒給 render」的寫法 |
| **預設參數 0 當成「沒有這份資料」** | `setPieceProfile(raw, games, { defenderGoals = 0 })` —— 而西甲的 build 沒有這份資料(它沒有球員層的逐球位置),於是 20 隊全部輸出「後衛進球 0、佔比 0%」。舊版面看不到那一欄,戰術頁合併之後它就印在畫面上:**整季後衛一球都沒進**。這是「0 是一個看起來很像答案的數字」第二次,機制不同:上次是算出來的 0,這次是**函式簽名裡的預設值** | 預設值一律 `null`,衍生的比例也只在有值時才算。前端的欄位判斷(`!= null`)會讓整欄自己消失。寫預設值的時候問一次:**這個預設值如果被當成答案印出來,讀者會相信嗎?** |
| **兩套版面的代價不是「多寫一次」,是看不見的漏掉** | 戰術頁兩套版面(英超 / 西甲)合併時才發現:西甲的 `shapes.json` 裡 **20 隊都有官方先發陣型**,而舊的西甲分支從頭到尾沒讀它 —— 資料躺在倉庫裡、畫面上一個字都沒有。跟「西甲頭貼本來就有 727 張」同一類 | 一套版面、每個區塊(以及每個欄位)自己判斷資料在不在。聯賽專屬的事實(頁首那段話、xG 出處、各隊頁有什麼、還要讀哪幾份產物)由各 build 寫進 meta,前端只負責畫。**合併的時候要逐份產物問一次:這個聯賽有而沒被畫出來的是哪些?** |
| **探測找錯欄位,而它只印一個看起來像結論的 0** | 問「盃賽有沒有球員評分」,第一版看 `p.rating`、第二版改成深搜「鍵名含 rating」—— **兩次都印「有評分 0 人」**,而上游其實給了:評分在 `entry.stats[].stats[].{key:'rating_title', stat:{value}}`,也就是某個 `key` 欄位的**值**,不是鍵名。我差一點照它寫下「盃賽沒有球員評分」 | 探測問的是「**管線讀得到什麼**」,所以直接 import 管線自己的函式(這裡是 adapter 的 `flatPlayerStats`),不要另外攤一份。「對不上永遠是安靜的」—— 收完要數一次配對率,而且數出 0 的時候先懷疑自己找錯欄位 |
| **一個 `git add` 兩個路徑,其中一個不存在 → 整個 add 什麼都沒加** | 盃賽回填第一次跑:抓到 **64 場、0 退回**,回寫那一步印「raw 沒有變化,不提交」,整個 job 報 **success**。原因是 `git add -f A/ B/` 裡 B 還不存在(第一次跑只有一個盃賽的目錄),git 整個指令失敗、一個檔都沒加,而 `2>/dev/null \|\| true` 把錯誤訊息跟結束碼都吞掉 | 逐個判斷目錄存不存在再 add。這是 `continue-on-error` 那條坑的近親:**把失敗藏起來的寫法會讓「這件事沒做」看起來像做完了** |
| **新加的探測排在 workflow 前面 → 成功跑完但讀不到輸出** | 讀探測結果的方法是抓 job log 的**尾端**(整份 486 行),而排在第一步的輸出被後面四步擠出視窗;下載連結也試過,沙箱的 egress 擋掉 blob.core.windows.net(CONNECT 403)。症狀是「探測 success、4 個請求、然後什麼都看不到」 | 新探測排在 `probe-apis.yml` 的 latest job **最後一步**,規矩寫進檔頭。跟既有那條「印太多跟沒印一樣」是同一件事的另一面:**印的位置也算** |
| **抓取器整份覆寫,上游一頁沒回就把一筆資料弄丟** | LaLiga 官方教練那一步在 CI 印 `19/20 頁`(一隊的 staff 頁沒回),而抓取器是「抓到幾筆就寫幾筆」—— 那位教練從西甲名冊上整個消失,他那筆核對通過的前任期沒地方掛,而斷言比的是**總數**(掛上 16 vs published 17)→ `npm test` 紅 → 整個部署被擋。**本機一切正常**,因為本機沒有重抓 | 兩頭都要修:抓取器**逐隊**保留上一輪那一筆(只在真的有失敗時、只保留還在本季名冊裡的,標 `stale` + `staleSince`);斷言改成守「**名冊上有這一隊就一定要掛上**」,名冊上沒有那一隊只印一行不擋。而且**保留舊值就要講出來** —— `retrievedAt` 是這一輪的時間,拿它替一筆沒重新核對的資料背書就是說謊(標籤改印「官方核對 {日期}」)。這是「上游時差寫成 CI 紅線」的第三次,差別是這次**不只斷言太嚴,是抓取器自己把資料弄丟了**。凡是「重抓後整份覆寫」的地方都要問一次:**這一輪少抓到的那幾筆,是消失還是保留?** |
| **每個來源各取一筆再合併,不等於合併之後的前幾筆** | 「我的球隊」的接下來三場:聯賽那一份只取**一場**、盃賽與歐冠取全部,合併排序後切三場 —— 於是 Arsenal 印的是 9/15 聯賽盃 → 9/19 英超 → 10/13 歐冠,而 **10/10 的英超對 Leeds 整場不見了**(它從頭到尾沒進候選名單)。畫面上完全看不出來:三場都是真的比賽、時間照順序、機率也對,**只是中間少了一場**。是使用者對賽程才抓到的 | 「各取幾場」與「合併後留幾場」用**同一個常數**(`NEXT_N`),每個來源都取 N 場再合併。測試守的是**不會過期的性質**:排進前 N 的那幾場,時間都不晚於任何被排除的場次 —— 斷言「第三場是 10/10 對 Leeds」的話,下一輪踢完就紅(「把目標達成寫成 CI 紅線」)。跟「只讀了資料的其中一個區塊」同一家族:**這次不是漏讀一個區塊,是每個區塊都只舀一瓢** |
| **共用元件的圖像要先註冊才會是圖** | `compBadge` 有 logo 就畫真圖、沒有就退回色塊 + 縮寫(EFL / PL / UCL / LL),而 logo 在 `competitions.json` 裡。新頁忘了 `registerCompetitions` → 整頁都是縮寫色塊,而總覽與盃賽頁有註冊、是真圖 —— **同一個東西在兩頁長得不一樣,而兩頁都「正常」**。隊徽同理:本站不認得的球隊(歐冠的 Lille、盃賽的低級別隊)要走上游的查表(`ucl-teams.external` / `cups.crests`),不補的話同一排裡有些格子有隊徽、有些是光禿禿的文字 | 新頁用到 `compBadge` / 隊徽就要問一次:**這一頁註冊過了嗎?** 退回值設計成「不會壞、只是難看」的東西時特別危險 —— 它不報錯,而且只有跟別頁並排才看得出來 |
| **`?v=0` 這種假戳,`stamp-assets` 永遠命中不了** | `explore.html` 與 `predict.html` 的 CSS/JS 引用帶著 `?v=0`(手寫的佔位符),而 stamp 是用**字面** `'./x.js'` / `src="assets/js/x.js"` 去比對再加戳的,`strip()` 又只拿掉 8 位十六進位 —— 於是那兩頁的資產**從建立以來沒有換過版本戳**:core.js 或 app.css 改版時讀者載到的是快取的舊版。三個轉址頁的 `./core.js?v=0` 同病。而守資產戳的那條測試**只掃長度剛好 8 位的戳**,假戳它看不到 | 新加 import 一律寫成 `from './x.js'`(不帶戳),stamp 會補。`npm test` 多一條紅線:`?v=` 後面一定是 8 位十六進位(掃之前先剝註解,否則講這條規則的註解自己會誤報)。這是「import 那一行帶著版本戳」那條坑的**反面**:那次是戳對不上,這次是**戳根本沒被寫進去**,而兩次的症狀都是「畫面是舊的而測試全綠」 |
| **共用模組的具名 import 跨行 → 單檔版整份死掉** | `import { a, b, c }\n  from './x.js';` 在分頁版完全正常,但單檔版是用**單行正則**把 import 拆掉的,跨行的拆不掉 → 攤平之後留著一個 import 陳述句 → 整份單檔版是語法錯誤 | 具名 import 寫在同一行。`bundle.mjs` 自己有守門(「單檔版裡還有沒拆掉的 import」)會擋下來,**所以這條是被擋住而不是上線才發現的** —— 跟 `export { a as b }` 那條同一家族:**單檔版的打包是文字處理,不是真的模組系統** |
| **跨聯賽的一頁,球隊身分卻跟著「站在哪個聯賽」變** | 使用者只寫「聯賽杯 球隊隊徽」。實測:站在西甲打開聯賽盃,**27 支英格蘭球隊一支都沒有隊徽** —— 整排灰色三字母代碼方塊(es1 74 個、en2 36 個、pl 0 個,所以在英超看完全正常),而同一張表裡第九級的 Fleetwood Town 有真隊徽與全名。隊徽**一支不少都在倉庫裡**,是三個消費端都拿目前聯賽的名冊查:盃賽頁 `C.badge/C.name`(`team()` 查不到回的是**樁**不是 null)、總覽只查 `cups.json` 的 `crests`(那份**刻意只收本站沒有隊碼的球隊**,所以有隊碼的三個聯賽都沒有)、單場頁 `C.team(code)` 拿到樁的 `['#444','#888']` → 兩隊在球場圖與射門圖上同一個灰。歐冠那幾列早就做對了(`uclCrest` 先看 code) | 身分收在 `core.js` 的 `cupClubs()`(從**英超目錄**的 `clubs.json` 載)+ `cupCrest()` / `cupName()`,三個消費端共用。**「有隊碼但名冊查不到」要退回上游全名,不是退回隊碼** —— 那就是畫面上那個三字母。刻意不 `registerTeams`:隊碼跨聯賽重複(BUR),全域登錄後蓋前。兩條舊斷言原本**釘的正是 bug 本身**,改的時候要在註解裡寫明。跟「`link()` 的 league 沒給就繼承」同一家族:**跨聯賽的東西不能有「目前聯賽」這個隱含參數** |
| **標題宣稱「這些是英超球隊」,而表是用隊碼篩的** | 盃賽頁「英超球隊走到哪一輪」列的是「有本站隊碼」的球隊 —— 本站認得三個聯賽,所以 27 支裡有 Hull City、Leeds、Coventry、Sheffield United,而標題旁邊就寫著「共 27 支」(英超只有 20 支)。同一輪還有第二處:球隊頁盃賽區的足總盃說明寫 `C.league() !== 'es1'`,句子裡還寫著「英超球隊」,站在英冠看英冠球隊的頁面就是這句 | 「前端把聯賽的事實寫死」的第三種:寫死的不是數字也不是「我們只做 X」,是**「這一批是誰」**。篩選條件是什麼,標題就寫什麼(「本站認得的球隊」);二元式一律換成集合。發現它的方法還是那一條:**改完真的把頁面開起來看** —— 這兩句在站上都很久了 |
| **共用模組用 `export { a as b }`,單檔版一開就掛** | 單檔版的打包是把共用模組的 `export ` 字首**剝掉**再攤平,所以 `export { expandable as uclExpandable };` 剝完剩下 `{ expandable as uclExpandable };` —— 語法錯誤,整份單檔版死掉,**而分頁版完全正常**(2026-09-13 加歐冠單場頁時踩到) | 共用模組(`SHARED` 那幾個)一律 `export const` / `export function`,要改名就寫 `export const 新名 = x => 舊名(x)`。`npm test` 有一條掃 `^export {`。同一輪還踩了兩個舊坑:**import 那行帶版本戳、字面字串 replace 靜靜沒命中**(加具名匯入時),以及**頁首在註冊球隊之前就畫**(隊徽印成上游的數字 id) |
| **同一份資料分兩個區塊,而其中一個「這一季還是空的」** | 「只讀了資料的其中一個區塊」第二次,這次在前端而且**測不出來**:歐冠一季的場次分聯賽階段(`leagueMatches[]`)與淘汰賽(`rounds[].ties[].legs[]`),四個地方只讀了前者(我的球隊、總覽兩處、我的預測)。**本季 `rounds` 是 0** —— 淘汰賽二月才有,所以九月四個地方的結果跟正確寫法**一模一樣**,截圖、`npm test`、`sweep` 全綠。二月起才會每一場淘汰賽靜靜掉隊。發現它的不是讀程式,是核對腳本順手印的那一行「淘汰賽 0 場」 | 走整份收一次收在 **core**(`uclSeasonMatches`),四個地方共用 —— 四份裡**唯一做對的那一份**(`ucl-view`)不會去修正錯的三份。測試要拿**有那個區塊的舊賽季**驗函式本身(144 + 45 = 189),只驗本季等於什麼都沒驗。跟著來的第二層:**淘汰賽的 legs 也有 `matchday`**(1 / 2 = 首 / 次回合),照 `matchday ? '聯賽階段第 N 輪'` 寫會把十六強標成「聯賽階段第 1 輪」。凡是「這一季還沒有」的區塊,都要問:**它出現的那一天,誰會發現?** |
| **存在使用者瀏覽器裡的鍵,是已經發布的契約** | 「我的預測」的紀錄鍵是 `賽季\|主隊\|客隊`,而歐冠同一組對戰一季可以踢兩次(聯賽階段 + 淘汰賽,主客方向還相同)—— 2025-26 撞 7 組、2024-25 撞 6 組,撞到的兩場**共用一筆預測**。但那個鍵是 localStorage 的物件鍵:**直接改格式的話,使用者填過的預測全部變成對不到的孤兒**(`scorePredictions` 直接 continue、卡片變回空白),而且一個錯都不報 | 改鍵一律用**加法**:`keySuffix` 只給會撞的那一批(歐冠淘汰賽給 stage),沒有它的場次鍵一個字元都不變。測試要同時驗**帶了不撞**與**不帶真的會撞** —— 只驗前者的話,哪天資料本來就不撞,這條就變成在守一件不存在的事。凡是會寫進 localStorage / 網址 / 匯出檔的識別字,改之前先問:**已經存著舊格式的人怎麼辦?** |
| **「這一輪」在淘汰賽裡不是一個數字** | 歐冠淘汰賽的 legs 沒有輪次語意:`stage` 是階段、`matchday` 是**首 / 次回合**(1 / 2)。拿 matchday 當輪次的話十六強首回合會跟聯賽階段第 1 輪混在同一輪,而畫面完全正常。而且**單場決勝的決賽上游兩季寫法不同**:2025-26 是 `null`、2024-25 是 `0` —— 寫「null 才不標」就會印出「第 0 回合」 | 階段接在聯賽階段後面編號,**順序照產物的 `rounds[]`**(lib 已經排好,前端不要抄第二份);編出來的號碼**只排序不印**,名字一律從 `rounds[].zh` 讀(連聯賽階段那幾輪都給名字,前端就一個字都不用硬編)。回合標記只認 1 與 2,其他不標 —— 跟進球子代碼同一條規矩。驗的方法是**把未來預演一次**:本季 `rounds` 是 0,照現況把頁面打開等於什麼都沒看到 |
| **「上游有問題」的第一反應,三次都是我自己的量測錯了** | 接英冠球員層那一輪,同一天連錯三次:(1) 球員進球加總對不回該季總進球 —— 我把**沒有 FotMob 資料的 7 場**也算進分母;(2) 牌的獨立核對只涵蓋 162 組裡的 24 組 —— 我用 `T.codeOf` 而 build 用的是 `byFd`(隊名在 `fd` 欄位);(3) 由 (2) 推出「英冠 14 隊對不到隊碼、風格位移大半資料被丟掉」,而 build 的 log 明明寫著 24 隊。三次的第一反應都是「上游資料有問題」 | 每一個「對不上」先問**我的分母 / 我的鍵跟被比較的那一邊是不是同一批**。既有的 build 已經在做同一件事時,直接用它那一份對照器(不要自己另外拼一個)。這是「我數不出來 ≠ 上游沒有」的第三、四、五次 —— 它不會因為寫進文件就不再發生,所以**斷言上游有錯之前,先把自己的量測拿給既有管線對一次** |
| **模組層的 `const` 宣告在檔尾 = 暫時死區,而頂層程式已經在跑** | 球員頁新的渲染器把位置代碼表 `const POS_ZH_AGG` 宣告在檔尾(就在它自己的函式旁邊),而頁面的頂層 `try` 在模組執行時就呼叫那個渲染器 —— 函式宣告會提升、**const 不會**。症狀:整張表格不見、篩選器空的,只有 console 一行 `Cannot access 'X' before initialization`,而 `npm test` 全綠(它看不到版面) | 渲染器用得到的模組層 const 一律宣告在**檔案最前面**(import 之後)。跟「第一次繪製之前 await 一個跨網域請求」、「頁首在註冊球隊之前就畫」同一家族:**直線腳本的原始碼順序就是執行順序**。發現它的方法還是那一條:改完真的把頁面開起來看 |
| **Obsidian 的球員檔名撞名有第二層:同一個人、同一個隊碼、兩個聯賽** | 球員檔名撞名時加隊碼區分(既有做法)。英冠球員層接上之後出現 52 組**加了隊碼還是一樣**的:升班的球隊(Coventry)上季在英冠、本季在英超,同一個人在英冠那一份是上季紀錄、在英超那一份是本季紀錄,兩邊 `teamCode` 都是 COV → 兩則筆記同檔名。產生器的守門擋下來了,所以是**被擋住不是上線才發現** | 加隊碼之後再檢查一次,仍然撞的加聯賽後綴(`Jake Bidwell (COV)・英冠`),順序照 `LEAGUES` 讓排前面的聯賽保持原名 —— 改名會斷掉手寫筆記裡既有的連結。跟球隊筆記那條(`Burnley(英冠)`)是同一條慣例,只是球員多一層:**球隊的隊碼在一個聯賽裡唯一,球員的「隊碼」跨聯賽會重複** |
| **剪接不是踢球:每一種剪接單獨看都合理,合起來畫面上的人「根本沒有在踢球」** | 模擬遊玩的正常 / 快為了壓進 8~12 分鐘用了三種剪接:離得遠的人直接放到該在的地方(cutTo)、沒結局的回合一格跳到終點且持球者換成對手(finishInstant)、射手放到射門點 4 m 外再射、停球 4 倍快轉。`npm test` 全綠 —— 它守的是事件不漏、射手對得上、球在網裡,**看不到人是怎麼到那裡的**。使用者看到的是「瞬間換位、莫名掉球、無故進球,根本沒有在踢球」 | 演的段落一律連續、真人速度、不剪接任何人;省時間只靠**整段跳過**(淡出 → 站位 → 淡入,黑幕上寫跳到第幾分鐘,跳過的回合事件照記)—— 剪接要讓讀者看得出是剪接。規劃器抽成純函式(`game-playback.js`)才測得到;`npm test` 剝掉註解掃三個剪接函式的名字。「時間不夠」的解法是少演幾段,不是把每一段演假 |
| **位置層的三個指標都說沒抖,畫面卻在抖** | 使用者回報「球員不自然抖動」。我先量位置:相鄰兩格反向的來回只有 0.07 次/人/分 —— 位置層根本沒有高頻抖。真正在抖的是 `draw()` 裡的步態左右擺(`sin(stride) × 0.55 m`,全速 ±4.8 px、2.9 Hz),**它只在畫的時候加上去,位置層的任何量測都看不到**。另一半是低頻的「不自然」:目標點上疊的正弦抖動讓沒有人停過(站著 1.6%),每個人整場繞著一個點漂 | 量測要量**眼睛看到的那一層**:draw 加上去的位移也要算進去(或直接掃 draw 的常數)。「站著的比例」是新指標 —— 真人大半時間站著或走,一個 1.6% 的數字本身就是答案。修閒置走位時**門檻要兩個**(起步 1.5 m、停步 0.4 m):單一門檻那版反而讓翻轉從 3.4 升到 5.3,站在門檻邊上的人被會飄的目標拉來拉去 |
| **全速追一顆停在腳邊 2 m 的球,永遠繞圈** | 動畫改成照劇本演之後,接球者站在球邊 2 m、速度 5.2 m/s 整整四秒控不到球,每一腳傳球都逾時。加速度有上限,全速的轉彎半徑 v²/a ≈ 4 m,比控球距離大 —— 他不是不想去,是轉不進去。在位置層量「有沒有靠近」看不出來,要看逾時計數 | 要轉彎就先減速:速度跟目標方向夾角越大,目標速度越低(背對時剩 15%,煞停再轉)。凡是「加速度有上限」的運動模型都有這個半徑,靠近一個要轉向的目標時一定要減速 |
| **追空中球跑向球,不是跑向落點** | 一記 50 m 的長傳,接球者朝球跑,在半路迎上一顆 21 m/s 的球(速度太快控不住),球飛過他落在身後 20 m,回頭要兩秒 —— normal 模式 68 次逾時全是這個。逾時紀錄印出來才看見:球停著、接球者以全速在別的方向 | 空中球用 loft 還會飛幾秒算落點,追球的人跑去落點(`landingOf`)。「對不上永遠是安靜的」的動畫版:每一段演出都要有逾時計數,而且逾時當下的狀態要印出來,不然只看得到「很慢」 |
| **上一回合結束時挑好的主罰者,在同一個停球被換下** | 回合制引擎在角球贏得時就挑好主罰者,而自動換人在下一個回合開頭的停球做 —— 那個人被換走,傳球串的第一個人不在場上(seed 3 回合 168)。畫面上退回「離球最近的人」代演,看起來完全正常;是「回合裡碰球的人都在場上」那條測試抓到的 | 跨回合帶過去的人,開回合時要再確認一次還在場上,不在就重挑。任何「先決定、後執行」中間隔著一個會改名單的步驟,都要在執行時重驗 |
| **畫面的狀態與引擎的狀態是兩份** | 畫面當主時鐘之後,引擎已經算到未來幾秒(它產回合時就決定了結局),畫面只演到現在。統計面板讀引擎的 stats 會**提前洩露**還沒演的射門與紅牌;陣容面板讀引擎的 onPitch 會在紅牌演出來之前就少一個人 | 頁面另外維護一份只含演過事件的顯示狀態(`disp`),所有數字都讀它;唯一讀引擎的是不會洩露的東西(λ、換人合法性)。「跳到結果」也走同一條:整串事件套進 disp,不是換一條路徑 |
| **測試的最小間距把被罰下的圓點算進去** | 紅牌的人 `off = true` 留在原地(不畫、不動),別人走過他身上就量到 0.10 m,「球員最近也保持 0.9 m 以上」紅了 —— 舊台子沒有紅牌所以沒踩過 | 量測的母體要跟畫面一致:只算在場上的人。跟「掃描把兩種假象當成 bug」同一家族,第一次量到的數字要追到原因再信 |
| **兩個名字都像「被封阻射門」的欄位,本站從第一天挑錯那一個** | FotMob 球隊統計有 `shot_blocks` 與 `blocked_shots`;對照表把前者當被封阻的射門,單場頁就這樣印了三個月。跟總數對得上(全聯盟做的封阻 = 被封阻的射門),不拋錯。階段 C 把對照表以外的值存下來、寫一條「兩個互比」的核對,380 場裡 203 場主客對調才相等,才發現前者是**自己做的封阻** | 判決用**逐顆的資料**:射門圖每一顆有 blocked 旗標,`blocked_shots` 760/760 對上、`shot_blocks` 只有 77%。舊快取不重抓,讀取器用射門圖回推(`fixBlockedShots`)。跟「上游欄位名不等於它的單位」同一家族:**兩個相似的欄位,要拿第三份資料判**,不是挑名字比較像的那個 |
| **加第四個聯賽時,少寫一個產物欄位 → 整頁 TypeError,而 `npm test` 全綠** | 德甲的 `meta.model` 漏了 `simulationRuns` —— 球隊頁的總覽說明直接寫 `meta.model.simulationRuns.toLocaleString()`,整頁炸掉只剩一行字;同一輪還漏了 `analysis.json`,首頁 console 印「讀取 analysis 失敗(404)」、`#app` 只有 93 個字元。兩個都不會讓任何測試變紅(測試看不到版面),是**把頁面開起來**才看到的 | 新聯賽的 `meta.model` 逐欄位跟既有聯賽比(`test-bundesliga.mjs` 第 3 節),前端會讀的每一份產物逐一釘死(第 7 節)。**沒有內容的產物要寫空的而不是不寫** —— 404 會讓前端走「還沒 build」那條訊息,那是錯的。這是「產物的欄位名是跟前端的約定」的第二次,第一次是英冠同一輪犯四次 |
| **手寫的聯賽清單,加第五個聯賽時沒有人會記得回來改** | 資產戳那條的 meta 清單漏掉英冠,它的 meta 少了戳三個月沒有人知道 —— 而那條的註解**本來就寫著「新增聯賽時這裡要跟著加」**。加德甲時又找到三份同樣的寫死清單:`prob-history` 產物、`fixtures` 欄位對照、`sweep-pages` 的聯賽表。新聯賽在這三處**靜靜不被檢查**,而畫面完全正常 | 全部改成掃 `web/data/leagues/` 目錄(`stamp-assets.mjs` 本來就是這樣寫的,是測試沒跟上)。註解寫著「以後要記得改這裡」的地方,**那句話本身就是它該改成自動的證據** |
| **數出現次數的斷言,加一個聯賽就紅在「多了一個」** | 「我的預測掛在每個聯賽」那條寫成 `(core.match(/'explore', 'predict'\]/g)).length === 2` —— 加德甲變 3,測試紅,而它想守的事**完全成立**。跟「把目標達成寫成 CI 紅線」同一家族:那次是斷言「一定要有球隊沒評分」,補齊了就紅 | 逐個對象問(`Object.values(LEAGUES).every(l => l.open == null \|\| l.open.includes('predict'))`),不要數。附帶一條:**沒有 open 清單 = 全部開放**(英超就是這樣),不是「沒掛」—— 第一版沒分這兩種,英超就變成失敗 |
| **vault 的檔名撞到歐冠那一邊,而且連結與寫檔各算一次** | RB Leipzig 在歐冠是「本站認不得的球隊」(那一層的身分來自 football-data,還沒接德甲隊碼),德甲名冊現在有它 → `德甲/球隊/RB Leipzig.md` 與 `歐冠/球隊/RB Leipzig.md` 同名。修的時候只改了連結那一張表,**寫檔那一行還是自己算 `sanitize(e.name)`** → 守門報「連結指不到任何筆記」,而它不會說是誰算錯的 | 檔名只能有一個來源(`uclExternalFile` / `teamFileByCode`),連結與寫檔都讀它。這是「Obsidian 的檔名跨聯賽會撞」的第三層:第一層是兩個聯賽、第二層是同隊碼的球員、這一層是**聯賽 vs 歐冠外部球隊**。真正的解是把德甲接進 `loadUclSeasons` 的身分來源 —— 但那要等隊徽交付,不然歐冠頁會從「上游的真隊徽」退回灰方塊 |
| **對帳還沒做完,結論先寫在畫面上** | 德甲接上逐場事件的同一輪,我在界線裡寫「逐場事件含烏龍球 → 球員榜與積分榜的差額**現在對得起來了,不必再只回報**」。隔天把資料拉下來一算:2026-27 缺口 4 顆、事件只涵蓋 **19/27 場**、抓到 3 顆;2025-26 缺口 20 顆、事件涵蓋 **0/306 場**、抓到 0 顆 —— 上一季一場都沒有,那 20 顆沒有任何東西可以對。那句話是在資料還沒到的時候就寫好的 | 對帳要有**兩個條件同時成立**才敢講對上:事件涵蓋**全部**場次、而且缺口剛好等於烏龍球數。不成立就把三個數字(缺口、抓到幾顆、涵蓋幾場)都印在畫面上說「還在對帳中」。跟「無法核對 ≠ 不一致」同一條:**涵蓋不完整的那一季不能下結論**,而「我期待它會對上」不是量測 |
| **counts 裡寫死的 0,而前端直接把它印出來** | 德甲 `counts.players` 從球隊層那一輪起就是字面 `0`(當時真的沒有球員),球員層接上之後沒人回來改 —— 球員頁的標題寫 `${meta.counts.players} 名註冊球員`,於是那一頁印著「**0 名 2026-27 註冊球員**」而底下列著 856 人。`npm test` 原本只對 `counts.crests` 一個 | counts 的每一個數字都要有一條「對得回產物」的斷言,不是只對想得到的那一個。**寫死的 0 就是在畫面上編數字**(鐵則一),而且它比 null 危險 —— null 會讓欄位消失,0 看起來像答案。這是「0 是一個看起來很像答案的數字」的第三次 |
| **共用版面裡寫死聯賽的名字** | `page-analysis.js` 的頁首印 `西甲 ${f.season}・第 N 輪`,而那一段是「不是英超」的那條分支 —— **英冠(從上線起)與德甲的每一個單場頁都寫著「西甲」**,而頁面其他地方完全正常、一個錯都不報 | 「前端把聯賽的事實寫死」的第四種:寫死的既不是數字、也不是「我們只做 X」、也不是「這一批是誰」,是**聯賽自己的名字**。一律從 `LEAGUES` 註冊表讀。`npm test` 掃共用版面裡「聯賽名後面緊接著插值」的寫法(掃之前剝註解 —— 講這條規則的註解自己就寫著那幾個名字) |
| **掃原始碼的測試加了「這一行要有反引號」,而樣板字串是跨行的** | 上一列那條測試的第一版:先過濾「這一行含反引號」再找聯賽名 —— 而出事那一行是 `<p>西甲 ${f.season}…`,**它上面一個反引號都沒有**(反引號在好幾行之前的 app.innerHTML 那一行)。把 bug 原樣貼回去,測試照樣綠 | 新加的斷言要做**負向對照**:故意把 bug 貼回去,看它是不是真的會紅。「加完是綠的」不是驗收 —— 它跟「這條測試守不住任何東西」長得一模一樣。逐行掃的前置條件只能用**那一行自己一定有**的東西 |
| **flex 的換行優先於收縮,所以長名字會把隊徽擠到自己一行** | 球隊卡的 `.row` 是 `flex-wrap: wrap`,名字那一塊的 max-content 一超過剩餘空間,**整塊掉到下一行、隊徽單獨留在上面**,卡片比鄰居高一截。實測 1280px:英超 1 張、**英冠 10 張**(副標多了球場名)、德甲 1 張 —— 是德甲的 Borussia Mönchengladbach 讓它現形的,但它三個聯賽都有 | 給會長的那一塊 `flex: 1 1 0`:理想寬度是 0 就永遠湊得下,文字改在自己裡面折行(只給 `min-width: 0` 不夠,shrink 輪不到)。**量測也要小心**:第一版用「隊徽與隊名的 `top` 不同」判換行,而 `.row` 是 `align-items: center`,文字兩行時 top 本來就不一樣 —— 修好之後數字一個都沒變,看起來像修沒用。要比的是**垂直有沒有重疊** |
| **log 讀不到的那一段,用「應該長這樣」補上去 = 編數字,而且編進了紀錄裡** | 探測跑完寫提交訊息,我要引兩個聯賽各取樣的那一場當證據。job log 的尾端只到法甲那一段(讀 log 是抓尾端,義甲第 3 節在更前面被擠出視窗),我**沒有把它讀出來就寫了「義甲 Inter 2 - 1 Torino」** —— 實際上是 `Genoa 0 - 0 Lecce`。那是一場不存在的比賽,而且寫在**提交訊息**裡:那是給下一個人的證據,不是隨手的敘述 | 鐵則一沒有「只在畫面上」這種例外:**提交訊息、文件、註解裡的數字一樣不准編**。引用 log 就要真的把那一段調出來(尾端不夠就加大 tail 或分段讀),讀不到就寫「這一輪沒讀到」。抓到之後用 amend 改掉那一則,不要另開一則說明 —— 留著等於讓假的比分永遠留在 git log 裡。跟「新加的探測排在 workflow 前面 → 讀不到輸出」是同一件事的下游:**那條坑講的是輸出會被擠掉,這一條講的是被擠掉之後我做了什麼** |
| **同碼不同隊:新聯賽的隊碼撞到別的聯賽,而守它的斷言只比英超** | 義甲法甲的名冊第一版編了 BRE(Brest)、BOL(Bologna)、MIL(AC Milan),而 **BRE 在英超是 Brentford、BOL 在英冠是 Bolton、MIL 在英冠是 Millwall**。跨聯賽的頁面(盃賽、歐冠)是拿隊碼查身分的,撞到就把兩隊的資料混在一起 —— 而 Bolton 與 Millwall 都會出現在足總盃 / 聯賽盃頁。守這件事的斷言**只跟英超比**,所以三組裡只抓到 BRE 那一組 | 比**每一個**聯賽,不是只比英超。同碼**同隊**(升降級的球隊同時在兩個聯賽的名冊裡)是既有現象、不算撞,所以比的是隊名不是隊碼。新聯賽編碼之前先跑一次全聯賽對照。負向對照驗過:把 MIL 貼回去會紅在 `MIL AC Milan vs 英冠 Millwall FC` |
| **把「德甲」換成參數時,有二十處落在單引號字串裡** | 抽共用 build 時把字面的聯賽名換成 `${L.zh}`,而其中 20 處在**單引號字串**裡 —— 樣板插值在單引號裡是字面文字,畫面上就印 `${L.zh}`。**不拋錯、只印垃圾**,`node --check` 也是綠的(語法完全合法),跟 `[object Object]` 那條同一類 | 批次換字面值之後要掃一次「`${` 出現在非反引號字串裡」。更硬的一道:**掃產物**——寫一支走訪器把每個聯賽的每一份 JSON 的每一個字串走過,找 `${`。掃程式會漏(註解、多行串接),掃產物不會:它看到的就是畫面會看到的 |
| **註解寫著「這不是失敗」,而它下面那條斷言正在把它當失敗** | 共用測試的「兩個獨立來源重疊場次比分一致」要求 `compared > 0`,而**上面那段註解本來就寫著「沒有 D1 的季不是失敗 —— 沙箱出不去」**。德甲剛好本機有舊季的 D1,所以矛盾了三個月沒人看出來;義甲法甲本機一場重疊都沒有,這條就紅在「沙箱抓不抓得到」上 | 註解跟斷言矛盾時,**先假設註解是對的**(它寫的是意圖)。紅線只放在「真的對不上」:有重疊就必須全對,一場都沒有只印一行。同一輪還有第二條同病:「回測母體是完整一季」寫死 `隊數 × (隊數-1)`,而法甲 2025-26 的 openfootball 真的少一場比分 —— 改成兩邊夾(不少於主來源供得出來的、不多於一整季),這樣「我們自己漏了場次」還是會紅 |
| **一個 `git add` 列 67 個路徑,新聯賽的目錄要第一次抓成功才存在** | 回寫那一步是單一 `git add -f` 加 `2>/dev/null \|\| true`。新聯賽的 `understat-*` / `fotmob-*` 目錄要**第一次抓成功之後**才存在,而抓取那幾步是 `continue-on-error` —— 抓失敗 → 目錄不存在 → git 整條失敗、**一個檔都沒加**,而錯誤訊息跟結束碼都被吞掉,job 照樣報 success | 逐個判斷存不存在再 add,略過的印出來。這是「盃賽回填抓到 64 場卻印『沒有變化』」那條坑的第二次,而這次是**在它發生之前**先修的:在本機用臨時 repo 重現過 —— 舊寫法列一個不存在的路徑加入索引 **0** 個檔、新寫法 3 個檔。**加新聯賽就是在這條列表上加會延遲出現的路徑**,所以它遲早會中 |
| **新聯賽的「缺什麼」不可以照抄上一個聯賽** | 英冠的說法是「Understat 不涵蓋這個聯賽」—— 實測過的否定,永遠不會有。德甲是五大聯賽之一,Understat **有**它,只是沙箱的出口代理不放行。抄過去就是告訴讀者「以後也不會有」,而那是假的 | 「拿不到」與「還沒抓」是兩句不同的話,產物裡分開寫,`npm test` 守著新聯賽不准出現「不涵蓋」那種說法。同理德甲的升降級附加賽是**跨聯賽**的(第 16 名 vs 德乙第 3 名),不可以照抄英冠的 `promotionPct`(聯賽內四隊互打)—— 語意不同,抄過來就是一個假數字 |
| **本機綠、runner 紅:斷言把「沙箱抓不到」當成了資料的性質** | 德甲上線後 `npm test` 在 CI 紅在 fixtures 欄位契約 —— `scoreSource` 的已知清單寫死 `['es1','en2']`,而 runner 上 `de1:fetch` **抓到了** football-data.co.uk 的 D1,補比分那一步就給德甲的場次標上 `scoreSource`。**本機一定是綠的**:沙箱的出口代理不放行那個網域,本機的德甲從來沒有補過比分,那個欄位根本不會出現 | 清單不要列聯賽,要寫**它真正的意思**:這個欄位的分界線是「主來源是不是官方 feed」,而那只有英超。改成「英超以外的都可以有」,下一個聯賽就不會再紅。凡是「哪幾個聯賽有這個欄位」的清單,先問一次:**這個欄位是聯賽的性質,還是沙箱抓不抓得到的性質?** 修之前先在本機把 CI 的狀態重現出來(手動給德甲的場次補上那個欄位再跑同一段),確認報的就是那一條 —— 不要照著猜的原因改 |
| **只看了管線的一半就宣布「資料被丟掉了」** | Understat 的 `team_title` 對季中轉隊的人是 `Augsburg,Mainz 05`,抓取器查不到隊碼,那幾筆都是 `code: null`。我當場寫下「資訊在上游,只是被丟掉了」並寫進了變更紀錄與這張表 —— **而 build 端的 `lib/adapters/understat-players.mjs` 本來就會自己拆那個字串**,西甲產物裡那 13 個人都帶著 `teams` 與 `multiTeam`,一筆都沒有掉。那支適配器的註解甚至寫著跟我一模一樣的顧慮 | 斷言「資料沒了」之前,**把消費端也打開看一次** —— raw 裡缺一個欄位不等於畫面上少一個人。這是「我數不出來 ≠ 上游沒有」的同一家族,只是這次兩邊都是本站自己的程式。真正該修的是另一件事:整串丟進 `codeOf` 會讓 `Augsburg,Mainz 05` 被報成**一個對不上的隊名**,跟「某一隊的 alias 真的缺了」長得一模一樣,而那份清單是新接聯賽時唯一的信號(英冠 14 支就是靠它)。**清掉警告的雜訊,不是救回遺失的資料** |
| **一對一對得起來,不等於對對了** | 德甲的 Understat 隊名 26 個裡只對上 11 個,剩下 7 個短名(Hoffenheim / FC Cologne / Freiburg…)剛好對上名冊裡還沒被認領的 7 支 —— 一對一、每個都沒有第二個可能的對象。很想就這樣收工 | **一對一是線索不是證據。** 本站在租借姓名那條坑上付過代價(「姓氏唯一就配對」對到完全不同的人)。所以 alias 補上之後還要一道獨立核對:逐隊把球員進球加總跟積分榜的該隊進球對帳,對不上的印出來。而且 alias 的來歷要寫進名冊(哪一支探測、哪一天、log 怎麼寫的),下一個人才知道那不是憑印象填的 |
| **同一種東西有兩種合法形狀,而測試拿錯的那一種當標準** | 德甲接上球員層之後 `npm test` 紅在「空產物的形狀跟英冠一致」—— 因為德甲的 `leaders` 走 Understat(跟西甲同形狀),而英冠走逐場累加,**兩種都是對的**,前端本來就看 `leaders.source` 分岔。測試的前提在德甲還沒有球員層時是對的,有了之後就不對了 | 比形狀之前先問:**這個產物有幾種合法形狀?** 有兩種就按 `source` 分流,跟同一種的那個聯賽比。第一版我還自己給 leaders 取了形狀(boards 依賽季分組、source 寫小寫 `understat`),結果是「有資料但前端一個字都畫不出來」—— 那是「產物的欄位名是跟前端的約定」第三次 |
| **寫死的不是聯賽名,是資料來源** | 球員頁的來源那一句寫死「資料來源 Understat + SportMonks」。西甲確實兩個都有(SportMonks 補背號 / 頭貼 / 出生日期),德甲**只有 Understat** —— 於是德甲的球員頁印著一個這個聯賽根本沒用的來源,不拋錯、`npm test` 也看不到 | 由資料判斷(`leaders.sportmonks` 有東西才提它)。這是「同一句文案寫死『這是英超』」的同一家族,只是這次寫死的是**來源**。共用元件裡任何提到資料來源、供應商或官方機構的句子,都要問一次:**別的聯賽走到這裡會印什麼?** |
| **追一個殘差,連續兩次讀錯自己的資料形狀** | Understat 的球員進球加總比積分榜少 2%,我想證明那是烏龍球。第一次去讀 Understat 的情境檔,鍵讀錯,算出「20 隊全部對不上、總進球 178」(實際該是上千);第二次去數 FotMob 的烏龍球,拿 `e.team` 跟 `m.home`/`m.away` 比,全部得 0 —— 兩次都不是資料有問題,是我讀錯 | 這是「我數不出來 ≠ 上游沒有」的第六、七次。**停損點要先定好**:這個核對的目的是「隊名有沒有對錯」,而那個問題已經被逐隊分鐘(最低 0.947)回答了。殘差的成因是另一個問題,查不到底就**照實標成「只回報、不當結論」**(鐵則四),不要為了讓數字好看而硬給一個解釋 |
| **「只有一個」那句寫死在畫面上,第四次 —— 這次就在加了那個能力的同一支 build 裡** | 德甲的球員層接上、球員頁掛上導覽列之後,同一支 `build-bundesliga.mjs` 寫的 `meta.intro` 與 `boundaries` 仍然是「這個聯賽目前只做到球隊與比賽這一層 —— 還沒有球員數據、xG、陣容與傷停」。前三次的教訓是「加了能力要回頭 grep **別的頁**」,結果這次連**自己這一份**都沒回頭看 | 這種句子一律由資料決定(`hasPlayers ? … : …`),資料變它自己會變。加了能力之後的檢查順序是:**先自己這一份產物,再全站 grep**。順帶一提,補上去的界線要**連缺口一起講**:球員進球比積分榜少 0~11% 這件事沒有查證到底,就寫「只回報、不當結論」,不要因為版面好看而略過 |
| **兩個不同國家的頂級聯賽同名,照名字挑會挑到另一個國家** | 要接德甲的 FotMob 逐場,而 FotMob 的 GER 清單裡有一堆叫 Bundesliga 的東西(2. Bundesliga、Frauen Bundesliga、Bundesliga Qualification、Regionalliga…),**而奧地利甲也叫 Bundesliga** —— id 38,就在本站倉庫的 `ucl-league-teams.json`(at1)裡。照名字挑就會挑到它,**而它照樣回得出 18 隊、照樣有完整的逐場資料**:畫面不報錯、測試不會紅,只是整個聯賽是錯的 | id 一律**證明**,不是查到就用:從上游自己的 `allLeagues` 找國家代碼(不猜),再拿那個 id 抓一季賽程**逐隊比對本站名冊** —— 18 隊全對才算數,挑到別國的話一支都不會對上。這是「兩個名字都像的欄位要拿第三份資料判」那條坑的聯賽版:**名字相同的東西不只一個時,要用內容而不是名字去認**。順帶一提「英冠(id 48)有那五塊」也不能推到德甲,逐場詳情有沒有那幾塊要自己取樣驗 |
| **拆不開的合計丟掉 = 分子分母不同批;整個加上去 = 重複計算。同一段程式兩個方向各錯一次** | Understat 對季中轉隊的人只給**兩隊合計**(`Angers,Rennes`),本站的規矩是拆不開就不掛。逐隊對帳第一版把那些人整個丟掉:法甲 REN 的進球只算到 37 / 59(Esteban Lepaul 整季 21 球),義甲 CAG 的分鐘掉到理論上限的 0.898 —— **轉會越多的隊看起來越像對錯了隊**(分鐘比值最低的五隊正好是轉隊人數最多的五隊)。第二版把合計**整個加到每一隊**,又變成重複計算:德甲本來乾淨的 FCA / M05(正是 `Augsburg,Mainz 05` 那一串)反而被標成可疑 | **拆不開的東西算成區間,不是一個值**:下限是掛得上去的、上限再加上那些人的合計,真值一定落在中間;判的是「理論值有沒有落在區間裡」而不是「比值有沒有超過某個門檻」。隊名對錯的話**下限與上限會一起塌到 0**,鑑別力一點都沒少。容差用實測定:三個聯賽全季 56 隊區間上緣最低 0.995(紅牌與碼表差 ±0.5%),所以留 5%。這是「我的分母跟被比較的那一邊是不是同一批」的第六次 |
| **早季兩個來源涵蓋的場次不同批,而症狀長得跟「對錯隊」一模一樣** | 2026-27 德甲有幾隊 Understat 已經算到第 3 輪、openfootball 的賽果還停在第 2 輪 → 分鐘比值 1.5、球員進球超過積分榜。兩邊都沒有錯,但任何「比值該接近 1」的斷言都會紅,而且**同一份資料裡有些隊是 1.0 有些是 1.5**(看那一隊踢到第幾輪),看起來像資料亂掉 | 不同批的記成 `coverage` **只回報、不判**,跟「無法核對 ≠ 不一致」同一條。判定要逐隊算,不是整季一個旗標 —— 同一季裡兩種狀態會並存 |
| **手寫的聯賽清單第四次:這一份不能改成掃目錄,所以要改成守門** | `build-obsidian.mjs` 的 `LEAGUES` 是手寫的,加義甲法甲時沒有人回來改 —— vault 產出 8,022 則筆記、**0 個壞連結**,兩個聯賽整個不在裡面而畫面完全正常 | 它的 `wf`(走查回測逐場檔)檔名**沒有可以推出來的規律**(pl→backtest、es1→laliga、en2→championship、de1→bundesliga),掃目錄只會讓新聯賽拿到一個不存在的檔名。所以 `npm test` 拿 `web/data/leagues/` 逐個比對清單、並確認它指的回測檔真的存在。**「這一份推不出來」不是留著手寫的理由,是加守門的理由** |
| **「這一輪不看」被當成「這一季不要了」—— 抓取器整份覆寫的第二種死法** | 盃賽抓取器比賽中那一趟(`--live`)為了省請求把 `WANT` 縮成只有本季,而寫檔是拿這一輪的 `seasonsOut` 整份覆寫 —— 上季**沒進迴圈**,於是被寫掉:聯賽盃 2025-26 的 91 場連同 `cup-details/eflcup/2025-26/` 91 個檔一起消失。而 `missingSeasons` 是 `[]`(從迴圈的角度看什麼都沒缺),每一條失敗路徑都老實地 `if (old) push(old)` —— **問題不在失敗處理**。`npm test` 全綠,畫面只是少了一整季賽後報告 | 「這一輪沒去看的」要從快取**原樣帶過來**,而且帶完季數仍變少就**整份不寫**(第二層是給「以後有人改壞第一層」準備的)。「沒去看」與「看了沒拿到」要分開 —— 後者抓取器自己保留 old 並記 `missingSeasons`,在合併那一層再補一次會把「抓失敗」蓋成「一切正常」。規則抽成純函式才測得到(`lib/cup-seasons.mjs`),掃原始碼守不住這種。跟上游那條的差別:**那次是上游少回一頁,這次是我們自己把範圍縮小了** |
| **共用 build 裡寫死的「來源代碼」,第五、六處照抄德甲** | 抽出 `lib/build-league.mjs` 那一輪掃掉了四處照抄(隊數輪次、FotMob id、`D1`、`${L.zh}` 落在單引號裡),**而資料界線與頁尾來源那兩行還留著**:義甲法甲的首頁印著「openfootball(**de.1**)與 football-data.co.uk(**D1**)」、頁尾來源連到德國那一頁。不拋錯、`npm test` 也看不到版面 —— 是把義甲首頁**開起來看**才現形的 | 來源代碼跟隊數、輪次、FotMob id、升降級規則同一類:**都是這個聯賽的事實,一律由呼叫端給**(`L.ofCode` / `L.div`)。測試要守**兩個方向**:自己的代碼要在、別的聯賽的不准出現 —— 只守前者的話,把 `de.1` 貼回去那條照樣綠(產物裡本來就有 `it.1`)。順帶一提頁尾的網址不要憑印象填各國索引頁(本站只證實過 englandm / germanym / spainm 三個),指向**抓取器真的下載的那支 CSV** |
| **`since` 跟「第一次看到他」比,在只有本季資料時永遠得出幾百天** | 教練核對的第一版拿交付的 `since` 跟逐場資料裡「他第一場執教」比 —— 而本站的逐場只涵蓋本季,所以 2024 年上任的教練必然是本季開幕戰,差 **821 天**。那個數字什麼都沒說,而結果是**每一個久任的教練都產生一則沒有資訊的備註** | 比之前先問:**這個比較在什麼前提下才有意義?** 這裡是「他季中接手」——第一場晚於該隊開幕戰時,兩個日期才對得起來。久任的那些直接不比。「印太多跟沒印一樣」的又一次:一則對每個人都會響的警告等於沒有警告 |
| **核對門檻的鑑別力要實測,不能只讀它的說明** | 隊色 vs 隊徽的 ΔE 門檻是 40,說明寫著「抓的是差得離譜」。到底多離譜才擋得下來?**拿亮綠 `#00FF00` 當多特蒙德的主色(隊徽黃黑),ΔE 只有 20,照樣通過** | 用一筆**故意做壞的資料**去量門檻,量出來的數字寫進說明。不要因此就去調門檻 —— 調它要有真交付的證據,而且寫窄了會誤擋(英冠把容量上限寫成 62,000 誤標倫敦碗那次)。做得到的是**把最差的幾筆印出來給人看**:通過不等於對,尤其在沒有對照組的聯賽 |
| **接一條「即時路徑」要四個地方同時有它,少一個等於沒接** | 德義法接 FotMob 賽果:抓取器的表、`build-league` 把 `fotmobDir` 傳給 `leagueMatches`、`live-window` 的表、比賽夜 workflow 的聯賽清單。**只做前兩項的話 log 顯示有抓、產物顯示有接,而讀者看到的一點都沒變** —— 因為部署是 12 小時一次,不進比賽夜迴圈就沒有「當晚」可言。而 `live-window` 少一筆會回「不認得的聯賽」,迴圈**根本不會為它進場** | 接即時路徑之前先把**整條鏈**列出來,一路問到「讀者什麼時候會看到」。規劃時我寫的是「三行設定」,真做下去是四個地方 —— 估得太小不是問題,**把估計當成範圍才是**。三份手寫清單現在有 `npm test` 拿 `web/data/leagues/` 逐個比對 |
| **清狀態只清了一半,留下自己跟自己矛盾的組合** | 「模型不可以偷看未來」那道守衛把基準日之後的場次重設成 `played:false, fh:null, fa:null`,**但沒清 `scoreProvisional` / `scoreSource`** —— 那一場就變成「未賽、沒有比分,卻掛著『FotMob 暫定賽果』」。接第三來源時用假快照才現形(第一次跑出來是三場「暫定」但比分 null-null) | 重設狀態時要把**同一件事的所有欄位**一起清,不是只清想得到的那幾個。「真的上游不會那樣給」不是理由 —— 清狀態就是要把不一致的組合清乾淨。沙箱連不到的來源,**捏一份假快照把管線跑一次**比讀程式有效 |
| **測試釘死實作的寫法,重構之後紅在「行為沒變」上** | 兩條比賽日 workflow 的斷言比對字面的 `--league=es1` 與 `ACTIVE=$(node …live-window`。那支改成逐聯賽迴圈之後字面都不在了,而它們守的性質(有沒有問過 live-window、是不是只有明確 false 才收工)一個都沒少 | 斷言比**性質**不比寫法。釘死寫法的測試會在「重構但行為不變」時紅,而紅久了就沒有人看 —— 那跟「把會隨資料變動的數字當紅線」是同一個病。放寬之後一定要做負向對照:把它原本要擋的那個 bug 貼回去,確認還是會紅(這裡是把收工條件改回 `!= "true"`) |
| **掃描器把「跨聯賽的頁」只在英超開一次,而那幾頁的內容其實是逐聯賽的** | `sweep-pages.mjs` 的 `SITE` 把 overview / knowledge / allplayers / explore / cups / ucl 當成「內容跟聯賽無關」。而 `explore.html` 第一個分頁就 `C.load('knowledge')`,那份產物是**逐聯賽**的 —— 英冠德甲義甲法甲根本沒寫,四個聯賽點「探索」都 404,畫面停在「**載入資料中…**」不動(連錯誤訊息都沒有,比報錯更難判斷)。掃描器每次照樣回報「全部乾淨」,**是使用者自己點到的** | `SITE` 清空,每個聯賽都開(113 → 143 個目標)。**「這一頁跟聯賽無關」這種省事的假設,要拿它實際載入的產物去驗**,不是照頁面的用途分類。另外加一條 `npm test`:從 `LEAGUES` 的 `open` 清單讀「導覽列上有這一頁嗎」,有就要求那份產物存在 —— 守**配對**而不是「每個聯賽都要有」,哪天某聯賽不掛那一頁也不會變成假紅線 |
| **四個聯賽一起寫 null 交差,而其中一個的資料就在倉庫裡** | 補那四個聯賽的 `knowledge.json` 時,最省事的做法是 `numbers: null` 一次過。**但英冠的球員層有背號**(`shirt`,本季 547 人全有),它的背號分佈是真的算得出來的 —— 走 Understat 的德義法才是上游不給 | 「這個聯賽有沒有」一律**看資料裡有沒有值**,不是按聯賽寫死,也不是看欄位在不在(欄位在而全是 null 會算出一張每格都 0 的表,比沒有更糟)。這是「西甲的 shapes.json 20 隊都有而舊分支沒讀它」的第三次:**補空產物的時候,先逐個問一次這個聯賽是不是真的沒有** |
| **守「不准寫死聯賽名」的那條測試,自己的聯賽名單是寫死的** | 那條掃描器的 `NAMES` 寫 `['英超','西甲','英冠','德甲']`,加義甲法甲時沒有人回來改 —— 它**靜靜不再檢查新聯賽**,而它守的正是「新聯賽被印成別的聯賽」。同一天在 `fetch-crests.mjs` 抓到本體:開場白寫 `LEAGUE === 'es1' ? '西甲' : '英超'`,德甲義甲法甲跑起來全部印「抓取 N 支**英超**球隊的隊徽」 | 掃描器的清單也要從註冊表長出來(這裡是 `core.js` 的 `LEAGUES`)。**中間試過掃產物的 `leagueLabel`,結果少掉西甲**(它的 meta 沒有那個欄位)—— 比寫死還糟,所以換來源之後要印出來數一次。負向對照要拿**新聯賽**的名字貼回 bug:拿舊名單裡有的名字試,壞掉的版本也會紅,等於沒驗到 |
| **核對的定義換了,而畫面上那句話還在講舊的定義** | 資料界線印「分鐘都落在滿季理論上限的 95% 以上」—— 改成區間判定之後,區間**下緣**最低到 0.898,那句話既是寫死的數字也已經不是這個核對在量的東西了。同一輪還有「球員進球加總比積分榜少 0~11%」,那是德甲量出來的、被照抄給義甲法甲 | 改判定邏輯的時候要一起問:**哪一句話在描述這個判定?** 描述句一律講「實際做了什麼、量出來多少」並從資料算(`${nameJudged} 隊季裡…區間上緣最低 ${nameWorstHi}`),不要複述門檻 —— 門檻改了它自己會跟著變 |
| **照抄參數組時,把「要不要抓那份資料」一起抄了,而新聯賽沒有人讀它** | 逐場抓取器的熱區圖是**第二個請求**(一場的成本翻倍),旗標 `heat: false` 當時只給了歐冠與兩個盃賽。英冠德義法是照抄既有聯賽那一行加進去的,於是每一場都多花一個請求 —— 而熱區圖的兩個消費端(`attachPlayerTracking` 只在英超西甲的 build 被呼叫、`duel-anim.js` 只有模擬遊玩用而 `GAME_LEAGUES = ['pl']`)**在這四個聯賽都不存在**:1,338 / 856 / 1,023 / 966 人裡有 `tracking.heat` 的是 **0 人**。症狀是 0:不拋錯、畫面完全正常、測試全綠,只是每個比賽夜都在燒一倍的請求。真正咬到的是另一件事:要使用者點頭回填 992 場時,我照文件寫的「一場一個請求」去問 —— **實際是 1,984 個,拿一半的代價換到的同意不是對那件事的同意** | 「要不要抓」跟「抓得到」是兩件事:**有消費端才抓**。守它的測試不列聯賽清單 —— 從 LEAGUES 算「誰在抓」、從產物的 `tracking.heat` 與 `GAME_LEAGUES` 算「誰在讀」,在抓而沒人讀就紅(負向對照:拿掉 `de1` 的旗標會紅在「沒有任何消費端卻在抓」)。加新聯賽照抄參數組時,**每一個旗標都要問一次「這個聯賽有誰在用它」**,不要因為上一個聯賽那樣寫就那樣寫 |
| **「build」有兩個意思:npm script 是一串,直接指 `.mjs` 會少跑後面那幾支** | `local-sync.mjs` 呼叫 `scripts/build.mjs`,而 `npm run build` 是 `build.mjs && game/build-game.mjs && stamp-assets.mjs` —— 少跑的那兩支裡有寫資產戳的那一支,於是跑完 `npm run local:sync` **六個聯賽的 `meta.assets` 全是 undefined**(不是對不上,是不見了),`npm test` 紅 12 條。**那一支的註解從以前就在講資產戳的順序規矩,而它自己根本沒跑那一步**。同一支還有第二個洞:聯賽順序是手寫的,加義甲法甲時沒人回來改 → `local:sync` 只重建四個聯賽,兩個聯賽靜靜停在舊產物(「手寫的聯賽清單」第五次) | 要「建站」就走 `npm run build`,**「build 是什麼」只能有一個定義**,就是 package.json 裡的那一行。聯賽清單改成掃 `web/data/leagues/`,對照表用 package.json 的 `{聯賽}:build`(檔名推不出來:es1 → laliga、en2 → championship);不照慣例的列進明確清單,**兩種都不是的當場失敗**。這是「`npm run stamp` 那一行沒跑」的第三次 —— 發現它的方法不是讀程式,是**真的跑一次再看戳還在不在** |
| **批次改名用「名字 + 左括號」當錨,`.map(名字)` 那種就漏掉** | 把 `chainOf` 搬進另一個模組並改名 `chainBrief`,用 `.replace('chainOf(', 'chainBrief(')` 批次換 —— 而 `.map(chainOf)` 後面**沒有括號**,原地留著一個已經不存在的名字。`node --check` 綠(語法完全合法)、`npm test` 13 條全綠,瀏覽器一開就是 `chainOf is not defined`,「跳到結果」整條死 | 改名之後 grep **舊名字**確認一個都不剩,不要只看新名字有沒有出現。這是「字面字串 replace 靜靜沒命中」的同一家族,差別在**錨挑錯了**:識別字的用法不只「後面接括號」一種。發現它的方法還是那一條:改完真的把頁面開起來看 |
| **分頁列是開賽時寫一次的 innerHTML,所以後來才長出來的那一頁沒有按鈕** | 「賽後解讀」只在完場後掛出來,完場時程式把 `tab` 切過去、面板畫的**就是**賽後解讀 —— 而分頁列還是原本四個、沒有一個是選中的。讀者看不出自己在哪一頁,點走之後也回不去 | 「東西在但沒有按鈕」的第四次,而這次的機制是**一次性渲染 vs 之後才變的狀態**:清單會隨狀態改變的 UI 一律寫成一支會重畫的函式(連同綁事件),不要寫進只跑一次的那份樣板。守它的斷言比**性質**(那份一次性樣板裡沒有分頁標籤),不是某支函式叫什麼名字 |
| **回填了上一季的 raw,而消費端只走本季的賽程 → 看起來像回填沒有用** | 德甲回填 306 場之後,build 的 log 寫著「FotMob 逐場統計:**333 場**(2025-26、2026-27)」,而下一行的烏龍球對帳仍然是「2025-26 事件涵蓋 **0/306** 場」。raw 進來了、`loadFotmobMatchStats` 也載了,卡在最後一步:對帳數的是**賽後報告裡的事件**,而報告的迴圈走 `fixtures` —— 那是**本季的賽程**,上一季的場次一場都不在裡面。不拋錯、測試全綠,而畫面上那句「還在對帳中」會永遠掛著 | 迴圈改走 `[...lastMatches, ...curMatches]`,並且**把「建立母體」與「發布範圍」分開**(報告產物仍然只發布本季 —— 上一季的報告會讓產物大十倍,那是另一個決定)。新資料進來之後要問一次:**誰在讀它?那個消費端的母體跟著變寬了嗎?** 測試要守兩頭,只守發布範圍的話把母體改回 `fixtures` 照樣綠 |
| **放寬一個共用的集合,而它不只一個消費端** | 為了讓烏龍球對帳數得到上一季,把賽後報告的建立母體從「本季賽程」放寬成兩季。對帳對了 —— 而英冠的 **AI 賽後文章**跟著從 83 篇變成 **633 篇**,因為那個產生器吃的是同一個 `reports` map,而 `analysis.json` 也是首頁與單場頁**整份載**的。放寬的時候我只想著「誰需要更多」,沒想「誰會順便拿到更多」 | 放寬之前先 grep 一次**有幾個人在讀它**,逐個問「這一個該不該跟著變寬」。這次的答案是:對帳要兩季、逐場檔要上一季、內嵌產物與 AI 文章只要本季 —— **一個 map 三種範圍,就要有三個名字**(`reports` / `publishedReports` / `archiveReports`)。抓到它的是既有的一條斷言(「賽後篇數等於賽後報告數」),那條測試原本守的是別的事 |
| **上季才有的球隊沒有隊徽,而名冊查得到它** | 發布往季賽後報告之後,降級的球隊在本季 `teams.json` 裡沒有、`clubs.json` 有 —— 但 `clubs.json` 裡的那幾支**沒有 crest**(隊徽只抓本季名冊)。往季單場頁就是空的隊徽框,而同一頁其他地方都好好的 | `fetch-crests.mjs` 的 `--include-history` 本來就是為這件事寫的,發布往季資料時要跟著跑一次。抓不到的先**逐一試上游檔名**再下結論:`1. FC Heidenheim 1846` 一直 404,對的是 `1.FC Heidenheim 1846`(「1.」後面**沒有空格**);Pisa 是 `Pisa Sporting Club`。試出來的寫進 `crestSource`,不要塞進 `alias`(那是給球員層的隊名比對用的) |
| **場地的方向寫死在引擎裡,加了中場換邊之後整個下半場一球都進不了** | 進球判定寫 `ball.x < 0 ? 'home' : 'away'` —— 上半場完全正確。加了中場換邊之後,下半場每一顆射正都判成「射進自己那一邊」,`ball.shot.side === scoring` 永遠不成立 → 全部記成門將撲救。症狀是**進球 0.50 : 1.00 而 λ 是 1.99 : 0.70,射門數卻完全正常** | 「前端把聯賽的事實寫死」的引擎版:寫死的是**場地的方向**。一律從進攻方向算(`(ball.x < 0) === (H.att > 0)`)。凡是「左邊 / 右邊」「主隊那一側」這種話,都要問一次:**換邊之後它還成立嗎?** |
| **同一件事有兩個計數器,而它們的定義不一樣** | 射正:引擎只要球在門框範圍內過線就 +1,於是解圍與折射後的鬆球都算 —— 一場 33 腳射門報 **18 次射正**(真實約 9)。而畫面是從事件算的,「進球 + 撲救」加起來只有 **13**。兩個數字都在同一個程式裡,差了 5 | **自己跟自己對不上就是線索**:差的那 5 次就是「過了門線但它已經不是一腳射門」。修的是定義(射正只算真的是射門的球),不是讓兩邊互相遷就。畫面上的每一個數字只能有一個來源 —— 為此還補了 `block` / `save` 事件,免得面板要同時讀事件與引擎計數器 |
| **比賽變長了,而每分鐘的率沒跟著降** | 加中場與補時之後一場從 90 分鐘變成 95.5 分鐘,而扣扳機的機率是「每次決定」的 —— 射門從 28 漲到 36.1(真實 30.4)、進球 3.00 對 λ 1.99(**+3.8 SE,錨沒守住**)。改的是比賽長度,紅的是射門率 | 凡是「每場幾次」的真實值,錨的是**一整場**;改了比賽長度就要回頭重掃那些率。反過來也一樣:調了率之後要確認比賽長度沒變。發現它的方法是 `npm run game:sim` 把兩個數字印在同一行(長度與射門數)—— **會互相影響的量要印在一起** |
| **不是係數調錯,是一個行為根本沒寫** | 兩黃罰下的紅牌一隊一場 0.5 張(真實含直接紅牌才 0.05)。第一反應是「黃牌率調太高」—— 而黃牌數是對的(3.4 對 2.9)。缺的是:**吃過黃牌的人會收手**,真的球員被警告之後會避免再犯 | 率對了而結果不對的時候,先問**少了哪一個行為**,不要去調那個已經對的率。補上「吃過牌的人犯規率 ×0.15」之後 20 場 0 張紅牌。順帶一提那個 0.15 也是掃出來的,而且要講清楚真實值含直接紅牌、模擬只做兩黃,所以模擬應該落在真實值**以下** |
| **測試守著一件已經不存在的事** | 換掉播放方式之後,「跳過的回合事件照樣回報(skipSeq)」「每個回合結算都要記簡記」這兩條紅了 —— 而它們守的東西(剪接式播放的回合)在新引擎裡**根本不存在**。還有一條「目錄裡每個 `game-*.js` 都要在單檔版清單」,會讓任何一支**退役的**模組紅在「它不在清單裡」,而它本來就不該在 | 重寫一個子系統的時候,舊測試分三種:(a) 守的性質還在 → 改寫法;(b) 守的東西不存在了 → **刪掉或改守新的性質**,留著就是一條永遠紅的假紅線;(c) 列舉式的清單 → 改成**從入口走訪**(這裡是從 `game-view` 的 import 追),列舉的那種每退役一個模組就要改一次。而且要補**反向**的那一條:退役的模組不准還留在清單裡 |
| **算了標準誤,但沒問標準誤自己準不準** | 連續引擎的 λ 錨驗的是「N 場平均落在 λ 的幾個 SE 內」。同一份引擎、同一個錨:**3 場**量到 −4.0 SE 並印「錨沒守住」、**12 場**量到 −2.5、**30 場**量到 −0.6。假警報的那一行看起來跟真的一模一樣,而我還拿 12 場那組數字寫進了紀錄 | SE 是從樣本估的,樣本小的時候**它自己在跳**。凡是「差幾個 SE」的檢定都要一起印出**這個樣本驗得出多大的偏差**(3×SE):3 場是 2.65 球,等於什麼都驗不出來。場數不夠就只印不判(跟 SE=0 的那一條同一套:**沒有定義**與**很糟**是兩件事)。而且兩個版本要在**同一批種子、同樣場數**上比 —— 拿 12 場的舊數字比 30 場的新數字,會把噪音算成改善 |
| **跑動距離看不見「呆站」——在原地繞圈也會累積距離** | 使用者看模擬的回報是「球員幾乎呆站在一個位置」,而我的指標寫著跑動 117 m/分(真實 113)、**看起來完全正常**。換成量**位移**才現形:三十秒走 53 公尺、只離開原地 8.7 公尺(路徑/位移 **6.1 倍**),球隊重心整場只移動 38.6 公尺。同時衝刺次數是真實的 **3.7 倍** —— 一兩個人在狂奔、其他二十個在原地走,兩件事同時成立而且互相掩護 | 量「有沒有在跑」要量**位移**(固定窗口的淨位移、路徑/位移比)與**球隊重心的移動範圍**,距離只能回答「累不累」。這是「量測要量眼睛看到的那一層」的第三次(前兩次是步態左右擺、剪接)。凡是使用者說「看起來不對」而指標說正常的,**先問這個指標量的是不是那件事** |
| **看起來是「人不動」,根因在傳球成功率** | 離球的人沒事做的三個原因裡,前兩個是陣型不跟球走、只有一個人逼搶 —— 修完只好了一半。第三個才是上游:**傳球成功率只有 61.7%**,球權每幾秒就換一次,所以沒有一次進攻發展得起來。不是他們不想跑,是沒有東西可以跑 | 把**失敗分類**再找根因,不要猜:我先猜失準、再猜折射,而印出來是「接球者就在四公尺內卻被別人拿走」佔 **76%** —— 控球權由「誰離球近」決定,而真實足球裡傳到位的球**接球者本來就佔便宜**。順帶抓到傳球選人**完全沒看傳球路線上有沒有人**:一記穿過兩個後衛的直塞會拿到滿分 |
| **分佈錯了,而平均值可以用一個係數湊對** | 射門數與進球都偏高,第一反應是把 xG 的係數調小 —— 那樣每球平均 xG 會對上,而**射門的離門距離分佈整個是錯的**:模擬平均離門 12.6 m、0~10 公尺佔 46%,真實(倉庫裡 10,631 顆 FotMob shotmap 座標)是 16.4 m 與 24% | 有逐筆資料的時候就**比分佈**,不要只比平均。真正的錯在形狀:扣扳機的機率原本 ∝ q(太陡),改成 q^α 掃出 α = 0.7,射門數與平均離門距離**同時**對上,剩下的水準差才用一個量出來的係數補。順序不能反 —— 先用係數湊平均的話,分佈永遠不會被發現 |
| **問題在一腳,而我去調了全域常數** | 射門在門前滾到停(力道 18~26 m/s 固定、滾地摩擦 5.5 m/s² 只滾得動 29.5 m,而射程上限是 30 m)。第一反應是「摩擦太大」,掃了 3.0 / 1.5 / 0.8:射門 23.5 → 19.0 → 13.7(真實 30.4)、界外球 28.5 → 33.5 → **101** —— 全部更糟。摩擦同時管傳球、鬆球、出界,而壞掉的只有射門那一腳 | 先問**這個常數還有誰在用**。局部的錯就在局部修:射門初速改成跟傳球同一條式子解出來(`v² = 到達速度² + 2×摩擦×距離`),需要多快就踢多快。掃描本身沒有白做 —— 它是**否定的證據**,把「摩擦太大」這個猜測擋掉了,那比直接改對更值得寫下來 |
| **側寫裡有這個欄位,而引擎從來沒讀過它** | 連續引擎從階段 1 到 2d 完全沒有讀側寫的 `possession` —— 五組配對量出來全部停在 50% 附近,而且**方向是反的**:目標 57.0% 的那一組跑出 47、目標 33.0% 的跑出 51。拿不到該有的球權就拿不到該有的進攻次數,強弱在畫面上被壓平成同一種比賽 —— 不拋錯、`npm test` 全綠、畫面完全正常 | 「這個欄位有誰在讀」要**逐個問一次**,不要等畫面不對了才回頭找。接上去的方式是**行為不是結果**:給每一隊一個從它自己真實控球率推的護球能力(相對值,自己的除以對手的),讓配對的結果自己長出來 —— 把控球硬設成目標值就是在畫面上編數字(鐵則一)。目標值要跟模擬值**並排印出來**,並講清楚哪個是輸入哪個是結果。這是「資料躺在倉庫裡而沒有人讀它」的第四次(前三次:西甲的 727 張頭貼、`shapes.json` 的 20 隊陣型、英冠球員層的背號) |
| **量測工具寫的時候是對的,直到有人加了一個新行為** | `npm run game:sim` 第一行印「瞬移(超過自己最高速)**263 次** 最大 98.33 m ← 引擎有問題」。追下去:3 場 66 次**全部在第 45 分**、每場剛好 22 次 = 全隊,最大 98.33 m ≈ 一個球場長 —— 那是階段 3 加的**中場換邊**把二十二個人鏡射到對面,依設計。那條量測寫的時候沒有換邊這回事;加了之後它從那天起每一次都印「引擎有問題」,而我照樣讀了好幾輪沒去追 | 加一個新行為之後要回頭問:**哪一條量測的前提被它打破了?** 排除要排**狀態換的那一格**(`s.half !== half`),不是「第 45 分」—— 補時會讓分鐘不準。排掉一格的代價要寫進註解(那一格真的有 bug 就看不到)。跟「掃描把兩種假象當成 bug」同一家族,差別是**這次的假象是我自己後來造出來的** |
| **傳中是一記平射穿過人群,而「第一點」判的是球飛過誰身邊** | 角球只補回三分之一,我先去查爭頂的規則 —— 而錯的是**軌跡**:飛行時間寫 `cl(d/16, 1.0, 1.5)` 秒,算出來弧頂只有 **2.76 公尺**(= 頭球高度),所以球整段都在人頭高度上以 23 m/s 橫穿禁區。逐球量:71 個第一點裡 **67 個是防守方**在飛行 **69%** 處、離瞄的那個點**還有 11.6 公尺**切掉的。那不是爭頂,是被路上的人攔掉,而且**完全不報錯**:球飛出去、有人碰到、畫面正常,只是禁區裡等球的六個人從來沒有機會 | 弧頂用**幾何推**,不要憑印象給飛行時間:要讓球只在「還在角旗邊」與「到了目標區」兩段低於頭球高度,弧頂取 6 公尺 → vz = √(2g·6)、飛行 2.21 秒,兩端各只剩 4.3 公尺低於 2.6 公尺。再加「**只爭下降中的球**」(`vz < 0`)。改完第一點落在 **94%** 處、離瞄點 1.9 公尺,攻守 45:47。**「誰碰得到」的規則沒有錯,錯的是球飛的路徑** —— 判定寫在哪一層之前,先問球在那一層長什麼樣 |
| **瓶頸還堵著的時候掃參數,會掃出「這個參數沒有影響」,而那句話會被寫成永久結論** | 4f 掃 `CORNER_HEAD`(0.62 / 0.80 / 0.95)印出來**一模一樣**,於是我把「它幾乎沒有作用」寫進了變更紀錄與規劃的否定結果欄。而那時傳中在半路就被切掉,禁區裡根本沒有人搶得到第一點 —— **掃的是一條被堵住的路**。修好軌跡之後重掃(每個值 50 場):10.0 → 12.3 → 14.7 → 14.8%,單調而且 3.5 SE | 掃描的結論要寫成「**在什麼前提下**沒有影響」,不是「沒有影響」。上游改過之後,**回頭把靠它得出的否定結果一條一條作廢** —— 那些話留著會讓下一個人(或自己)不去試真正有效的那條路。跟「量測工具寫的時候是對的,直到有人加了一個新行為」同一家族:那次是量測的前提被打破,這次是**結論的前提**被打破 |
| **20 場的掃描沒有鑑別力,而四個值排出來的單調趨勢看起來像訊號** | 同一份程式、同一組參數,20 場量到 FromCorner **12.1%**、50 場量到 **10.0%** —— 分享率的 SE 在 20 場是 1.5 個百分點,而要分辨的差距就是這個量級。20 場那一輪還排出了漂亮的單調曲線(12.1 → 13.2 → 14.7),我差一點照它定案;同一輪的射門數也騙了我一次(20 場 25.7 / 預算 1.09,50 場 23.8 / 1.01 —— 我為此差點去調 `SHOT_URGE`) | 掃描要**先算這個場數驗得出多大的差距**,再決定跑幾場(`check-sim` 的 λ 那一行本來就這樣印)。分享率就用 `√(p(1−p)/n)`,而且**把 SE 印在每一格旁邊** —— 只印點估計的話,單調趨勢與雜訊長得一模一樣。四核平行跑四個值,50 場的成本跟 20 場的牆鐘時間一樣 |
| **死球的「來源」沒記是誰的,於是被對手領走** | 射門情境分類掛在 `pendingOrigin` 上,而它只記種類(`corner` / `throwin` / `freekick`)。角球被解圍、**防守方**自己控住開的那一串照樣領到 `corner`,他們接著反擊射門就被標成 `FromCorner` —— 場上完全相反的一件事,而畫面與測試都正常 | 任何「下一個人領走」的暫存狀態都要記**是誰的**:領的人跟死球同一隊才算,不同隊就是運動戰(而且誰領到都算領完了,這次死球的餘波結束)。跟「`link()` 的 league 沒給就繼承目前聯賽」同一家族:**一個沒有主詞的狀態,會被第一個路過的人認領** |
| **上游塞一個「不是球隊」的參與者,而它長得跟球隊一模一樣** | 盃賽下一輪抽籤先公布、上一輪還沒踢完時,FotMob 給的參與者名字是兩隊用斜線串起來(`Manchester City/Norwich City`),而且**有自己的 id、有 logo 網址** —— 所以「盃賽出現的每一支球隊都查得到隊徽」就紅在一個不是球隊的東西上,整個部署被擋。**比沒有隊徽更危險的是隊碼**:整串丟進 `codeOf`,寬鬆比對有機會對上 `Manchester City`,那個還沒決定的參與者就會被當成曼城出現在「走到哪一輪」表裡 —— 畫面完全正常,只是講了一件沒發生的事 | 不編身分(鐵則三):adapter 標 `tbd`、**不查隊碼也不給 logo**,畫面印「X / Y 的勝者」。認法匯出成一支共用函式,下游一律 `t.tbd ?? isCupTbd(t.name)` —— 抓取器把正規化結果存進 raw,舊 raw 沒有這個旗標,各寫一份 `includes('/')` 就會有人悄悄過期。這是「盃賽寬鬆比對會對錯球隊」的**第三次**(前兩次是第九級的 AFC Liverpool 與 Bournemouth FC)。**沙箱抓不到 FotMob,本機 raw 沒有那一輪,所以這條只在 CI 紅、本機永遠綠** —— 驗證只能捏假快照跑管線,負向對照是「拿掉守門那個 codeOf 回的就是 MCI」 |
| **接了一個旋鈕,不等於那個旋鈕是一個「戰術」** | 模擬遊玩把戰術四軸接上引擎,四個都接得動、λ 的錨也沒破(中性級數逐字相同)。但量下去:**寬度拉寬讓自己射門砍半、對手翻倍**(方向是反的)—— 因為傳球選人本來就對靠邊的隊友扣分,拉寬等於把自己人推進被扣分的區,那不是打邊路,是跟既有的懲罰項打架;**節奏 ±20% 的機率換來射門 26.7 對 8.0**(3.3 倍),因為 `decide()` 每 0.8~1.7 秒才一次,一次進攻只有幾個決策點,±20% 複利起來太兇。四個拉了會讓球隊踢得很爛的按鈕,比四個寫著「還沒接」更糟 | 接得動**不是**驗收,要逐軸量它**自己宣告會改的那件事**(側寫的 `effects`),而且看方向對不對、幅度像不像一個踢法。通過的才掛上去,沒通過的留在 TODO 並把量測表寫進註解當**否定的證據** —— 它同時指出了修法(懲罰項要跟著寬度縮、節奏的級距要像 `POSS_K` 那樣掃出來)。跟「沒有回測證據就不進模型」是同一條規則在遊戲那一側的版本:**這裡的驗收不是 RPS,是「它改的是踢法還是強弱」** |
| **在被呼叫的那一邊去讀一個「呼叫端上一行才剛清掉」的狀態** | 助攻寫在 `giveTo` 裡讀 `ball.passTo` —— 而呼叫端為了算攔截,**在叫 giveTo 的上一行**就把 `passTo` / `passSide` 清成 null。於是助攻永遠是 null:整場 **0 筆**,不拋錯、`npm test` 也看不到(它看不到版面,而這個連版面都沒壞) | 跨函式要傳的東西就**由呼叫端明講**(`giveTo(p, from)`),不要在被呼叫的那一邊去讀一個會被改動的共用狀態 —— 讀的時機對不對,從被呼叫的那一邊根本看不出來。發現它的方法是**量了一次**:助攻率印出來是 0.000,而「對不上永遠是安靜的」 |
| **拿幾場去掃一個「一場只出現 0.2 次」的東西** | 十二碼率的 `BOX_CARE` 掃 0.25 / 0.15 / 0.10 / 0.07 各 8 場,而 **0.10 與 0.07 印出來一模一樣**(都是 0.13/場)—— 8 場總共只會出現一兩球十二碼,那個樣本分不開它們。照著挑就是在雜訊裡挑一個數字 | 稀有事件不要掃**結果**,要量**跟它成正比的那個基準率**:把係數設成 1(完全不收斂)跑夠多場,用 Poisson 算 SE,再除回去 —— 30 場 59 球 → 1.97 ± 0.26,0.23 ÷ 1.97 = 0.117(±1SE 0.103~0.134)。跟「算了標準誤,但沒問標準誤自己準不準」同一家族:**這次是連掃描本身都沒有解析度** |
| **新機制加了 xG,而 λ 的錨是「總量」** | 十二碼接上之後,30 場的進球是主 2.43(λ 1.99,+1.8 SE)、客 1.07(λ 0.7,**+2.3 SE**)—— 兩個都在 3 SE 內所以判定通過,但**兩邊都往上**。一球十二碼是 0.788 xG,約等於七腳普通射門,所以它加進來的量不是「多一腳射門」那個量級 | 判定通過**不等於**沒有偏移:兩邊同向漂就要講出來,而不是看到 ✓ 就過去。要收的話**扣預算而不是壓機率** —— 真實的每隊 12.6 次射門本來就含十二碼,所以該從運動戰的射門預算裡把那一份扣掉,而不是把扣扳機的機率整體調小(那會連分佈一起改壞)。**沒查清楚之前不要為了讓數字好看去調係數** |
| **稀有事件:數「出現幾次」估不準,要數「風險」** | 十二碼的 `BOX_CARE` 校準完之後量到 0.37/場(目標 0.23),看起來像調錯了 —— 而 30 場只會出現 11 球,那個估計自己的雜訊就蓋過要量的東西。更早一次從 `BOX_CARE = 1` 量基準率再外推也失敗:**一場兩球十二碼會把比賽本身改掉**(進球多、重開多),線性假設在那個區間根本不成立 | 把每一格那個 `rng() < p` 的 **p 加起來** —— 它的期望值就是次數,而它是連續量,估得準得多。實測 `BOX_CARE = 0.12` 的期望是 **0.221/場**,目標 0.23,**本來就是對的**,0.37 是 Poisson 雜訊。而且要在**目標區間附近**量,不要跑到一個行為完全不同的極端去外推 |
| **引擎照一個量調、錨照另一個量驗** | 模擬的射門數系統性比預算高 **25%**(四組配對 1.17~1.35,不是巧合)。追下去扣扳機的機率**完全正確** —— 主客比 0.51 跟 expShots 比 0.51 一模一樣、機率上限一次都沒咬到。錯的是**校準的目標**:`SHOT_URGE` 當初對著「兩隊自己的 `sf` 相加」調(不看對手防守),而 λ 的錨用的是 `expShots = sf × 對手 sa ÷ 聯盟平均`(有看)。兩支防守好的隊碰在一起,前者高估一大截 | 系統性偏差要先**把式子拆開逐項量**(這裡是「射門數 = 機會次數 × 扣扳機機率」),確認哪一項才是錯的 —— 機率是對的就不要去動機率。改的往往不是數字是**目標**:調一個係數之前先問「我在對什麼校準,而驗收的那條線在對什麼?」兩個不是同一個量就永遠對不上。修完**驗收的那一行也要跟著改**,不然修好錨會讓那一行看起來壞掉 |
| **扣了預算,而該補上的那個來源補不滿** | 角球接上之後,照真實的 17.3% 把它的份額從運動戰的射門預算裡扣掉 —— 而角球機制實測只生得出 **7.2%**。預算被拿走 18.2%、只補回 8.1%,總量短 10%:30 場射門/預算從 0.99 掉到 **0.88**、主隊進球 **−2.9 SE**(而且是往**下**掉,跟上一輪往上飄剛好相反) | **補不上的那一份不可以扣。** 新來源要從預算裡分一份,前提是它真的供得出那一份 —— 供不出來就只扣供得出來的(這裡是十二碼),剩下那一點併進全域係數的校準裡。扣預算之前先問:**這個來源實測供得出多少,而不是真實世界佔多少** |
| **判定放錯層:那一層的閘門是為別的事設的** | 角球傳中的頭球判定放在「控球」那一段裡,而那裡有兩道閘門:球要落地(`z < 0.6`)、球速要慢(14 m/s 以下才控得住)。一記傳中飛 35 公尺、1.5 秒到,水平速度 23 m/s 而且大半時間在空中 —— **兩道都過不了,沒有人碰得到它**,而且完全不報錯:球飛過禁區、落地、門將撿走,畫面上一切正常 | **頭球不需要「控住」球**,所以它不該走控球那條路 —— 判定要放在那兩道閘門**之前**。加一個新動作時先問:**我掛的這一層,它的前置條件是為誰設的?** 同一輪還踩了第二個:用 `st.chain` 判斷「這是角球」,而傳中還在空中時串是關著的(開串的正是要繞過的那一條),條件永遠不成立 |

---

## 三、怎麼驗證(每次改動都要跑)

```bash
cd epl
npm test          # 走查回測 + 各聯賽與各主題的自我檢查區塊,零依賴(數量看它自己印的那一行)
npm run build     # 產生 web/data/*.json
npm run bundle    # 產生單檔版 dist/warroom.html
```

進球事件的 `description` 子代碼目前已見過 **`G`(一般)、`P`(十二碼)、`O`(烏龍球)** ——
`O` 已用名單核對過(踢進的人在對方名單裡)。這三種已經在單場分析頁的
進球時間軸上顯示。**沒見過的代碼一律不給分類**,原碼留在 `kindRaw`,
測試有一條守著:正式資料裡出現第四種代碼就會紅,要先核對過才放行。

`npm test` 會跑**六個聯賽**(`test.mjs` 英超 → `test-laliga.mjs` 西甲 →
`test-championship.mjs` 英冠 → `test-bundesliga.mjs` 德甲 → `test-serie-a.mjs` 義甲 →
`test-ligue-1.mjs` 法甲;後三支共用 `lib/test-league.mjs`),
區塊數與斷言數**不寫在文件裡** —— 每加一條測試就會歪(實測過:三份文件三個數字,全錯)。
`npm test` 最後會自己印一行合計。涵蓋:走查回測、模型 vs 市場、即時勝率、AI 報告層、
真人專家觀點、API-Football 完賽資料、官方名單球員對照、隊名對照、賠率去水錢、
兩隊配色(702 組對戰的 ΔE 與對比)、近況特徵、傷停與拿牌、對照條長、
資料缺口判斷、官方進球事件解析、進球子類型、逐場進球明細、
Understat 進球情境、SportMonks 欄位與賽後資料轉換、西甲球隊數據。

**西甲的 `official.json`(逐場正式先發)與 `fixtures.json` 是兩份產物,要一起重跑。**
先發快取到的場次比 openfootball 賽果先落地時,只重跑其中一份會讓
「逐場對回比分」那條斷言紅掉 —— 看起來像供應商資料錯,其實是 `fixtures.json`
還停在舊的一份、沒把新落地的賽果算進去,於是逐場比對時賽程說「未賽」。
`npm run laliga:build` 一次產出兩份,不要單獨手動改任何一份。
(這條被一次 rebase 弄丟過,補回來。)

**跨聯賽的頁面只能有一份資料。** 足球知識與歐冠兩邊都看得到,
所以轉換邏輯收在 `scripts/lib/`(歐冠是 `lib/ucl.mjs`),
`build.mjs` 與 `build-laliga.mjs` **各呼叫一次同一個函式**,產出的 JSON 逐字元相同
(`npm test` 有一條守著)。複製一份轉換過去的話,改了一邊另一邊會悄悄過期。

**往季的賽後報告是逐場檔,不是內嵌。** `reports.json` 是**首頁與單場頁整份載**的,
所以只內嵌本季;上一季走 `match-reports/{季}/{id}.json`(2026-09-16,六個聯賽 2,299 場、141 MB),
索引是 `reports.archive.ids`(只有 id)。跟盃賽的 `cup-details/` 同一條路、同一個理由。
兩件事不能忘:**逐場檔必須逐次建置位元組相同**(部署一天兩次整份重寫,有時間戳就每次塞
2,299 個新 blob),以及**檔名用場次 id、撞鍵的不寫**(英冠的升級附加賽會讓「季|主|客」不唯一,
挑一個 id 去命名等於把某一場的報告掛到另一場的網址上)。單檔版不打包它們,畫面要講出來。

**頁面清單有三份。** `core.js` 的 `SITE_PAGES` 與 `PAGES`,加上 `scripts/bundle.mjs`
自己那一份。**忘了改 bundle 那份的話,新頁不會壞 —— 只會從單檔版靜靜消失**,
而分頁版一切正常(加總覽頁時實際發生)。`npm test` 有一條守著:
bundle 的清單要涵蓋 `web/` 底下每一個 `.html`。

**導覽列有兩份清單。** `SITE_PAGES` 是跨聯賽那一組(總覽、足球知識、歐冠),
`PAGES` 是這個聯賽那一組。**同一頁兩邊都放的話,導覽列會出現兩個一樣的分頁** ——
左邊一個、右邊一個,而且不會有任何地方報錯。實際踩過,測試補了一條。

**跨聯賽的轉換與流程一律共用,不複製。** 這一輪抽了三份出來:
`lib/league-matches.mjs`(主來源缺比分時用獨立來源核對後補上,西甲 SP1 / 英冠 E1)、
`lib/backtest-runner.mjs`(走查回測的協議 —— 各寫一份的話,兩個聯賽的 RPS 差 0.01
就分不出是聯賽的差異還是實作的差異)、`lib/names.mjs`(姓名配對)。
`laliga-matches.mjs` 與 `backtest-laliga.mjs` 現在都只是薄包裝。
抽完要驗**產物逐欄位不變**(西甲回測除了 `ranAt` 之外完全相同,實際比對過)。

**模擬遊玩 2026-09-16 起跑連續時間引擎。** 頁面的那一條鏈是
`game-sim.js`(1/60 秒固定時步的引擎)→ `game-live.js`(轉接層,只翻譯不做模型決定)→
`game-pitch.js`(只畫現在這一刻)→ `game-view.js`(版面)。播放速度是**時間倍率**,
**一格都不跳過** —— 舊的剪接式播放(`duel-anim.js` 照劇本演、`game-playback.js` 決定演哪幾段、
`game-engine.js` 回合制引擎)頁面已經不 import,也不進單檔版清單;檔案還留著只因為
它們自己的測試還在跑。`npm run game:sim` 印的是引擎對回真實值的每一項
(λ 錨、射門數與離門分佈、犯規、牌、補時、跑動、衝刺)—— **引用就跑一次,不要抄文件裡的數字**。
新引擎還沒有的:十二碼、直接紅牌、助攻、進球情境分類、體能、自動換人,
以及六軸戰術裡的心態 / 寬度 / 節奏 / 直接度(只有壓迫與防線高度接得到)。**沒做的一律不放欄位、不放按鈕。**

**模擬遊玩是獨立管線,而且界線不只是「不要互相 import」(2026-09-03 的決定,2026-09-16 使用者再講一次)。** 完整的界線是:**遊戲式的東西不出現在真實的那一面** ——不只是不污染數字,是不跑在真實管線(`build.mjs` 那幾支)裡、也不畫在真實的頁面(單場分析、賽程、實時戰況)上。真實比賽、預測、動態跟遊戲**完全分開**,實際是實際、遊戲是遊戲。
2026-09-16 踩過:一支單場事件模擬跑在 `build.mjs`、畫在單場分析頁的勝率旁邊,**它確實不污染 λ**(驗過:模擬開 / 關兩版產物逐位元組相同)—— 但那不是重點,位置才是,整支已還原。審查這種東西時要問的第一個問題是**它會出現在哪一頁**,不是「它會不會改到數字」。
 `scripts/game/` 與 `web/assets/js/game-*.js`
只讀真實管線的產物與 raw、只寫 `web/data/game/`;真實管線**不可以** import 它們(宿主 `page-explore.js` 除外)。
它算的機率是遊戲模型,不是本站預測 —— 唯一的錨是「沒有改動時 λ 等於站上的 λ」。`npm test` 有四條守著。
**賽後解讀(2026-09-16,`web/assets/js/game-diag.js`)是遊戲自己那一份**,跟真實那一面的報告層
(`scripts/lib/report/`)不共用也不互相 import —— 搬過來的只有三條規則:
文章裡的每個數字要在 evidence 找得到(`unattested()`,測試拿八場真模擬逐條驗)、
回空陣列是正常結果、**門檻只能是兩隊互比或這一場的絕對次數**。
最後一條不只是統計上的講究:拿真實聯賽基準去評價模擬結果,就是把真實那一面的東西搬進遊戲。
那支**一行 import 都沒有**(純函式),`npm test` 守著。
它的逐場抓取器 `scripts/game/fetch-fotmob-epl.mjs` **六個聯賽加歐冠與兩個盃賽共用**(照 `LEAGUES` 表參數化;歐冠的隊伍身分是 football-data id,賽果讀 `ucl.json`)。**熱區圖只給有人讀的聯賽抓**(英超西甲),其餘 `heat: false` —— 那是第二個請求,一場的成本翻倍。它抓的 FotMob 英超逐場資料(`data/raw/fotmob-epl/`)有控球、逐射門 xG、事件與名單,
**也有跑動距離、衝刺距離、衝刺次數與逐人最高速度**(2026-09-03 重探:2026-27 每場都有、2025-26 有 282/380,
缺的集中在 11 座主場;2024-25 沒有;`probe-physical.mjs`)。所以「跑動沒有免費來源」這句**已經不成立**——
拿不到的是跑位軌跡(追蹤座標),不是跑動量。這批資料已接進真實管線(`lib/matchstats.mjs`)。

**`build` 一定是最後一個。** `stamp-assets` 掛在 `npm run build` 最後,
會把資產戳寫進**每一個**聯賽的 `meta.json`;任何一個聯賽的 build 排在它後面,
那個聯賽的戳就會被重寫掉,`npm test` 的「meta 記的戳跟實際檔案一致」會紅。
目前順序:`laliga:build` → `en2` → `de1` → `it1` → `fr1`(各自 backtest 再 build)→ `build`。
**`local-sync.mjs` 不再手寫這份順序** —— 它掃 `web/data/leagues/` 並用 package.json 的
`{聯賽}:build` 當對照表(檔名推不出來),不照慣例的列進明確清單,兩種都不是的當場失敗。
而且它跑的是 `npm run build`(三支的串接)不是 `scripts/build.mjs` —— 直接指後者會少跑資產戳那一支。

**先跑 `laliga:build` 再跑 `build`。** `stamp-assets.mjs` 掛在 `npm run build` 的最後,
會把戳寫進**每一個聯賽**的 `meta.json`;反過來跑的話,`laliga:build` 會把 es1 的
`meta.json` 重寫掉、戳就不見了,`npm test` 的「meta 記的戳跟實際檔案一致」會紅。
(workflow 裡的順序本來就是對的。**但 `scripts/local-sync.mjs` 原本是反的** ——
build → laliga:build,跑完 `npm run local:sync` 再跑 `npm test` 就會紅在資產戳那條。
2026-08-28 修正。這條規則不是只給「手動重跑的人」看的,寫成腳本的地方也要照。)

**資產有版本戳。** `npm run build` 會跑 `scripts/stamp-assets.mjs`,
依**內容雜湊**給 `web/*.html` 的 CSS/JS 與 JS 之間的 import 打上 `?v=`。
沒有它的話改版面部署上去,使用者會看到最難察覺的組合:
**meta.json 是新的、JS 是舊的** —— 頁尾顯示最新建置時間、版面卻是上一版。
手動改完前端沒重跑 build 的話,跑 `npm run stamp` 補上;`npm test` 有一節守著
「戳對不對得回檔案內容」。

**人工交付的租借資料要先過核對器,而且核對結果要跟得上收件匣。**
`loans-verified.json` 記著收件匣的 sha256;收件匣改過卻沒重跑核對時,
`loadVerifiedLoans` 回 `stale`,build **整批不掛**並印出原因,`npm test` 也會紅。
沒有這道守門的話,build 會拿舊的核對結果背書新的交付內容,而且不會有任何地方報錯。
`loans:verify` 已掛進 `local:sync` 與 `epl-live.yml`。
`data/manual/loans.json` 是**收件匣**(協作方交付的原始內容,裡面有已知是錯的);
`npm run loans:verify` 核對後產生 `data/loans-verified.json`,**build 只讀後者**。
直接讀收件匣等於把核對整個繞過去。判定分四級,發布的只有 `confirmed`(有獨立來源
正面確認)與 `consistent`(查得動的都沒有矛盾),而且**等級要跟著資料走到畫面上** ——
兩者對讀者的意義不同,不可以混成一句「有租借紀錄」。

**Obsidian vault 是產物,而且有一個不能刪的例外。**
`npm run obsidian` 產生 `epl/vault/`(10,898 則筆記,會隨資料變動:六個聯賽 + 歐冠 +
英格蘭盃賽 + 足球知識),比照 `dist/` 不進版控。
**它的聯賽清單是手寫的**(`build-obsidian.mjs` 的 `LEAGUES`),因為 `wf` 的檔名推不出來 ——
加義甲法甲時漏掉過,vault 照樣產出 0 個壞連結而兩個聯賽整個不在裡面。
`npm test` 現在拿 `web/data/leagues/` 逐個比對那份清單 ——
但 `vault/我的筆記/` 是使用者手寫的,`.gitignore` 特地留了 `!epl/vault/我的筆記/`。
產生器清空的範圍是**從這次要寫的路徑推出來的**(加新資料夾自動涵蓋),
**不要改成 rm 整個 vault**:那會把手寫筆記不可逆地刪掉,重跑救不回來。
另有一道斷言:產生器想寫進 `我的筆記/` 就中止。詳見 `docs/接手資訊.md` 的 Obsidian vault 一節。

**`continue-on-error` 只給外部來源用。**
分界線是「失敗代表什麼」:某個 API 不通不該擋住整條流程,所以抓取類的步驟容忍失敗;
但**本站自己算的東西失敗就是有 bug**,要擋下來。目前擋的六個:
`news:merge`、`laliga:backtest`、`tune:form`、`tune:situations`、`snapshot`、`npm test`
(六個都是零 fetch 的本地計算或守門)。

這條是 2026-08-28 補的,因為實測到兩件事被藏了很久:補抓頭貼從第一次 CI 就在失敗
(runner 沒有 Pillow)、資產戳那兩條每次都假紅 —— 三次執行都沒有人發現,
因為 Annotations 只寫「Process completed with exit code 1」,不說是哪一步。

**`npm test` 擋下來會連帶跳過後面的步驟**,所以傷停快照與資料回寫刻意排在它之前 ——
快照記的是「今天誰不能上」,漏掉一天明天補不回來(60 天累積會斷)。

**改前端一定要真的開來看。** 測試檢查不到版面 —— 用 Playwright 截圖
(`/opt/pw-browsers/chromium` 已裝好),分頁模式與單檔模式都要看。
分頁模式(`npm run serve`,port 5173)才是 GitHub Pages 實際部署的那個。
`npm run sweep` 會把每一頁 × 三聯賽 × 兩個寬度開一遍、只印有異狀的(2026-09-11 起);
改完前端跑它,比人手開 57 頁可靠。讀結果前先看腳本檔頭那兩個假象。

---

## 四、沙箱連不到外網

所有外部端點在開發沙箱都被擋。要實測資料源:寫成 `scripts/probe-*.mjs`,
加進 `.github/workflows/probe-apis.yml`,用 workflow_dispatch 跑,再讀 log。

**不要憑印象斷言某個 API 有什麼欄位。** 探測過的結果寫在
`docs/進球細節-規格與提示詞.md` 第一節。

抓取禮貌:使用者明確要求**不要大量爬網站**。
- 靜態檔下載(raw.githubusercontent.com)沒問題
- 官方端點的進球事件是**零額外請求** —— 既有排程本來就在抓那些 fixture
- 要一場一個請求的做法**先不要做**

---

## 五、分支與提交

- 開發分支 `claude/premier-league-analysis-platform-kc4btp`
- 預設分支 `claude/method-feasibility-analysis-60pwfp`(排程與部署跑這條)
- 做法:在開發分支提交 → push → 合併進預設分支 → push → 把預設分支合回開發分支

**合併衝突幾乎都在產物**(`data/backtest*.json`、`web/data/*.json`、
`data/availability-history.json`)。解法固定:

```bash
git checkout --theirs <那些檔案> && git add <那些檔案>
cd epl && npm test && npm run build && npm run snapshot && npm run bundle
```

**不要手動編輯那些 JSON。** 它們是產物,重跑就對了。

提交訊息用中文,寫清楚「為什麼」而不只是「改了什麼」。
踩到坑就把坑寫進訊息裡 —— 那是給下一個人的。

---

## 六、程式碼風格

- 原生 ES module,**零外部依賴**(不要引入 npm 套件)
- 註解寫**為什麼**,不寫「這行在做什麼」。特別是:某個門檻為什麼是這個數字、
  某個做法為什麼不用看起來更直覺的那種
- 前端不用框架。共用元件在 `web/assets/js/core.js`
- 資料源都走 adapter(`scripts/lib/adapters/`),介面看 `docs/接新API.md`
