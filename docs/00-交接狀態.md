# 00 交接狀態 —— 本地接手指南

最後更新:2026-07-13(雲端架構師 session)
工作分支:`claude/method-feasibility-analysis-60pwfp`

> 本檔給「接手的人或 AI 助手」看:目前做到哪、下一步是什麼、怎麼接。
> 每次交接或里程碑結束時更新本檔。

---

## 1. 目前進度

| 項目 | 狀態 |
|---|---|
| 四份製作文檔(01 架構 / 02 流程 / 03 交辦 / 04 提示詞) | ✅ v2.0 Unity 版定稿 |
| 引擎決策 | ✅ Unity 2D(2022.3 LTS+;曾用 Phaser,已於 commit `7cbbfb8` 移除) |
| M0-T1 Unity 專案骨架 | ✅ 完成(未經 Unity Editor 實機編譯驗證,見下方「已知風險」) |
| M0-T3 開機畫面(Bootstrap) | ✅ 完成(同上) |
| M0-T2 素材放置 | ✅ 素材解壓至 `unity/Assets/Art/Raw~/`(盤點見 `docs/licenses/assets-inventory.md`);Cubic 11 字型已就位(`Art/Fonts/Cubic_11.ttf`);授權截圖已存 `docs/licenses/` |
| M0-T4 API key | ⏸ **製作人決策(2026-07-13)暫緩**:先以 mock 模式開發(config.json 已設 `llmMode: "mock"`),玩法成形後再接 Anthropic API。M0 驗收改為 mock 模式下按「測試 LLM 連線」回罐頭 JSON 即可 |
| M0-V 驗收 | ⬜ 待 M0-T2/T4 完成後在 Unity Editor Play 驗證 |
| M1 全部任務 | ⬜ 未開始,第一個是 P-03 |

git 歷史重點:
- `897f0cc` 四份文檔初版(Phaser 版)
- `0602747` Phaser/Node 骨架(已淘汰,僅供考古)
- `7cbbfb8` **引擎改版 Unity**:目前的專案骨架 + 文檔 v2.0

## 2. 已寫好的程式(unity/Assets/Scripts/)

| 檔案 | 內容 | 備註 |
|---|---|---|
| `Core/SimClock.cs` | 模擬時鐘(純 C#):1 tick=1 模擬分,0/1/4/16 倍速,OnTick/OnNewDay | 可單元測試 |
| `Core/GameConfig.cs` | 讀 StreamingAssets/config.json;無檔時 mock 模式 | |
| `LLM/AnthropicClient.cs` | Messages API(UnityWebRequest):重試/記帳/每日預算保險絲 | 模型:haiku 4.5 / sonnet 5 |
| `LLM/LlmRouter.cs` | 唯一 LLM 入口:kind→模型路由、mock 攔截 | 提示詞模板在 P-06 補完 |
| `Agents/Persona.cs` | 人設資料類,對應 content/personas/*.json | |
| `Boot/Bootstrap.cs` | M0 開機畫面(OnGUI):時鐘/速度/LLM 連線測試/記帳板 | 場上唯一要手掛的元件 |

## 3. 已知風險(接手第一件事處理)

2026-07-13 本地接手時已修復兩個擋路問題(commit 見 git log):
- 補上 `unity/ProjectSettings/ProjectVersion.txt`(缺了 Unity Hub 無法辨識專案),版本鎖 6000.5.3f1(本機已裝)
- `Packages/manifest.json` 補列 `com.unity.modules.*` 內建模組(缺了 `UnityWebRequest`/`OnGUI` 等會編譯失敗)

**C# 腳本未經 Unity Editor 實機編譯**(雲端環境無 Unity)。接手後第一步:
1. Unity Hub → Add → 開 `unity/` 資料夾(2022.3 LTS+,首次開啟建索引需幾分鐘)
2. 看 Console 有無編譯錯誤;有就先修(語法目標是 C# 9)
3. 建空物件掛 `Bootstrap` → Play → 對照 README「快速啟動」第 6 步驗收

## 4. 本地 AI 助手的接手起手式

在 repo 根目錄啟動 Claude Code,第一句話直接貼:

```
請先讀 docs/00-交接狀態.md、docs/01-製作架構文檔.md、docs/03-交辦事項文檔.md。
你現在是本專案的開發 AI 助手,接續雲端架構師的工作。

第一件事:協助我完成 M0 驗收 ——
1. 我已用 Unity Hub 開啟 unity/ 專案,回報 Console 的編譯錯誤(若有)並修復。
2. 我的素材在 C:\Users\Gon\Desktop\素材,請幫我複製到 unity/Assets/Art/Raw/,
   並盤點內容(包名、像素尺寸 16/32、有無角色行走圖),把清單記錄到
   docs/licenses/assets-inventory.md。
3. 引導我完成 config.json 設定,並在 Unity 內驗證「測試 LLM 連線」。

完成後回報 M0 驗收清單結果(見 docs/02 的 M0 節),我們再開始 M1 的 P-03。
```

M0 驗收通過後,照 `docs/04-AI助手提示詞文檔.md` 的流程走:
【通用前置說明】+ P-03,一次一個任務,按 03 文檔的依賴順序。

## 5. 分工約定(接手後仍有效)

- **本地 AI 助手**:執行 docs/04 的任務、直接操作 Unity 專案與素材、每任務 commit + push 到本分支
- **雲端架構師(原 session)**:pull 之後做程式碼審查、修訂文檔、規劃里程碑——需要時回雲端 session 說「拉最新的審一下」
- **製作人**:Unity Editor 實機驗收、人設與事件卡創意、API key 與素材
- 文檔是唯一事實來源;任務完成要同步更新 03 文檔的狀態欄(⬜→✅)
